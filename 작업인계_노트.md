# 작업 인계 노트 — 정식 빌드1 (Claude Code 세션 1)

> 최종 업데이트: 2026-06-10
> 대상 브랜치: `claude/compassionate-cerf-pMiR0` → `main` 머지 완료 (커밋 `edbc66e`)
> 다음 세션에서 이 파일을 먼저 읽고 이어서 작업할 것.

---

## 1. 이번 세션에서 완료한 작업

### [Step 1] 버그 수정 (커밋 `2859249`)
CLAUDE.md 1순위 버그 14건 중 **12건 완료** (index.html 직접 적용):

| # | 내용 | 처리 |
|---|---|---|
| 1·C | 오늘 한 문장 슬라이드 점프 + dots 가림 | position absolute↔relative → CSS grid 스택 |
| 2·9 | 학습 설정 변경/재진입 무반응 | sq2 출제를 `학습설정.sq2` 기반 공통 렌더(`sq2_출제_렌더`)로 분기 |
| 3 | 유의어 변별 1개만 표시 | 보기 4개 전부 설명 공개 |
| 4 | 질문하기 입력창 높이 고정 | oninput 자동 높이 조절 + 전송 후 초기화 |
| 5 | 글꼴 UI 명칭 불일치 | 「글꼴」로 통일 (행·모달·토스트) |
| 6 | 계정 삭제 탭 없음 | 위험 구역에 2중 확인 절차 추가 |
| A | Mona family명 불일치 | `'Mona'` 우선 + 눈누 CDN 출처 표기 정정 |
| B | 폰트 모달 CSS 조립 버그 | style 조립 로직 정리 + 미사용 srcMap 제거 |
| D | fontTxt 초기값 하드코딩 | `curFont` 유령키 'gowun' → 'nanum_square', 초기값 교정 |
| E | 이의있음! 컨텍스트 미초기화 | closeObj 시 general 복귀 |

> **버그 7**(구글 계정 하드코딩)은 Step 2 Firebase 작업으로 흡수 완료.

### [Step 2] Firebase 구글 로그인 + Firestore 동기화 (커밋 `9241194`, 값 `edbc66e`)
- Firebase **Compat SDK 12.13.0** (`<head>`에 app/auth/firestore 로드)
- 구글 로그인 강제화, `onAuthStateChanged` → 기존 사용자 로드 / 신규 사용자 이름 입력 → 문서 생성
- `신규사용자_생성`: KNOWLEDGE 13-1 루트 문서 기본값 (전부 한글 필드)
- `사용자데이터_적용` / `사용자데이터_저장`: Firestore ↔ 런타임 사용자 객체 동기화
- 로그아웃(2중 확인) / 계정 삭제(Auth + Firestore 문서 삭제)
- 미설정/오프라인 시 콘솔 에러만 출력 (별도 폴백 없음 — 지시 사항)

### [Step 3] Grok 연동 준비 (커밋 `9241194`, 값 `edbc66e`)
- `grok호출(기능명, payload)` helper — Cloudflare Workers 프록시 경유
- 기능별 reasoning 레벨 (`GROK_리즈닝레벨`, KNOWLEDGE 4-1)
- rate limit 문구 "Oops, 그록이 뻗었어요!" (KNOWLEDGE 5)
- **개별 기능(문제생성·질문하기·이의있음 등)에는 아직 미연결**

### 설정값 적용 (커밋 `edbc66e`)
- Firebase config: 프로젝트 `llove-djsdj` (apiKey 등 6필드 + measurementId)
- Grok Workers 엔드포인트: `https://xai-api-llove.hypoqwer.workers.dev`

---

## 2. ⚠️ 설계 결정 中 — KNOWLEDGE/CLAUDE.md 미반영 (다음 세션 처리)

| # | 항목 | 이번 처리 | 비고 |
|---|---|---|---|
| 1 | `언어의뿌리학습수` 분리 (KNOWLEDGE 13 [검토 필요]) | **미분리 유지** | 최고 관리자님 결정 대기 |
| 2 | 신규 사용자 기본 글꼴 | `nanum_square` | 13-1 설정 데이터 |
| 3 | Mona 글꼴 family명 | `'Mona'` 우선 + `'Mona-Sans'` 폴백 | CDN 검증 불가로 미확정 |
| 4 | Firebase SDK | Compat 12.13.0 | KNOWLEDGE 기술 스택 미기재 |
| 5 | 버그 14건 처리 현황 | 12건 완료 (7=Firebase 흡수) | 29·31섹션 상태표 미갱신 |

---

## 3. ⚠️ 배포 전 필수 (Firebase 콘솔에서 설정 — 코드 밖)

1. **Firestore 보안 규칙** — 본인 문서만 접근 허용 (미설정 시 DB 전면 개방):
   ```
   match /users/{userId} {
     allow read, write: if request.auth != null && request.auth.uid == userId;
     match /{document=**} {
       allow read, write: if request.auth != null && request.auth.uid == userId;
     }
   }
   ```
2. **승인 도메인 추가** — Authentication → Settings → 승인된 도메인에
   GitHub Pages 도메인(예: `yyyyynny.github.io`) + `localhost` 추가
   (없으면 구글 로그인 팝업 차단됨)

---

## 4. ❗ 미검증 (이 환경 = 네트워크 차단 + 브라우저 없음)

실제 동작은 **전혀 테스트 못 함.** `node --check` 문법 검증만 통과.
다음 세션에서 브라우저로 직접 확인 필요:
- 구글 로그인 팝업 → 신규/기존 사용자 분기
- Firestore 사용자 문서 생성·로드
- 계정 삭제 (Auth + Firestore)
- Grok 호출 응답

---

## 5. 📦 미착수 작업 (향후 — 각각 별도 브랜치 권장)

- **Grok 기능 연결**: 문제생성·채점·질문하기·이의있음 멀티턴·구어교정 (KNOWLEDGE 4·5·33·34)
- **토큰 시스템 + UI** (KNOWLEDGE 32·35)
- **서브컬렉션 동기화**: 복습대기열·즐겨찾기·휴지통·히스토리 + 게임플레이 이벤트별 저장
- **reasoning_note** 생성·저장·표시 (KNOWLEDGE 33)
- **성장 상세 화면 `sg`** (KNOWLEDGE 17-1)
- (선택) Analytics 연결 — config에 measurementId 있음, SDK만 추가하면 됨

---

## 6. 핵심 코드 위치 (index.html, 세션 1 기준)

> 라인 번호는 수정 시 바뀔 수 있으니 함수명으로 검색할 것.

| 영역 | 검색 키 |
|---|---|
| Firebase 초기화·로그인 | `Firebase초기화` / `구글로그인` / `인증상태_변경` |
| Firestore 동기화 | `신규사용자_생성` / `사용자데이터_적용` / `사용자데이터_저장` |
| Grok helper | `grok호출` / `GROK_WORKERS_ENDPOINT` / `GROK_리즈닝레벨` |
| 설정값 | `firebaseConfig` |
| sq2 출제 분기 | `sq2_출제_렌더` |
| 유의어 변별 | `유의어변별_선택` / `유의어변별_렌더` |
| 계정 삭제 | `계정삭제_1단계` / `showConfirmModal` |
| 런타임 사용자 객체 | `const 사용자` |
