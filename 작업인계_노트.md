# 작업 인계 노트 — 정식 빌드1 (Claude Code 세션 2)

> 최종 업데이트: 2026-06-10 (세션 2)
> 작업 브랜치: `claude/beautiful-franklin-24xvkz`
> 다음 세션에서 이 파일을 먼저 읽고 이어서 작업할 것.
> ⚠️ **Grok 게이트**: `index.html`의 `GROK_활성화 = false` — 크레딧 미구매로 실호출 전면 봉인.
>    승인 없이 true로 바꾸지 말 것.

---

## 1. 세션 2에서 완료한 작업 (이번 세션)

### [A] 정령왕 JSON 연동 — β8 완료
- `예문데이터_로드()`: `data/정령왕_통합_v2.json` fetch → **유의어 변별 18건 / 오늘의 한 문장 7건 / 구어 교정 27건** 실출제
- fetch 실패(오프라인·file://) 시 기존 정적 데이터 폴백 유지
- `모드DB_로드()`: 모드별 JSON 6종 로드 — 현재 빈 파일, 채우면 `출제_분기()`가 자동 사용
- 구어 교정: 예문 출제 카드(`spkPrompt`) 신설 — 예문 제시 → 입력 → 모범답안·포인트 비교 → 예문당 1회 +60 EXP
- 「data/명언.json 분리 예정」모순 주석 정정 (KNOWLEDGE 24 기준)

### [B] 글꼴 미리보기 가사 2세트 — 완료 (이전 세션 누락분)
- `글꼴가사` 10종 × A/B 세트: 피차일반(음율) ↔ Betelgeuse(유우리) — 모달 열 때마다 교체
- 모달 하단 가사 저작권 표기 추가: 「가사 © 음율 (피차일반) · © ユイリ (Betelgeuse)…」

### [C] EXP·마스터리·학습일 + Firestore 저장 확장
- `EXP획득()`: 배율([꾸준한 발걸음] ×1.01~×1.22, α11) → 레벨업 루프(Lv.70 캡) → `{레벨, 현재EXP, 총누적EXP}` 저장
- 연결 지점: 퀴즈 정답 +20 / 플래시카드 알았다 +20·헷갈린다 +8 / 아재개그 +20 / 유의어 정답 +20 / 구어 교정 +60
- `마스터리증가()`: 모드별 학습수(13섹션 한글 변수명) + 총누적어휘수, 풀 때마다 저장
- `학습일갱신()`: 연속학습일·총학습일·마지막학습일 (하루 1회)
- 설정 저장: 테마·글꼴·히스토리필터·AI문제만·복습상한 변경 시 Firestore 저장 + `plx_테마`/`plx_폰트` localStorage 캐시
- 학습 데이터 초기화 실구현 (2중 확인, KNOWLEDGE 15 범위 — 테마·글꼴·계정·즐겨찾기 보존)
- 「다음 문제」 버튼 실동작 (랜덤 재출제 — 기존 데모 안내 모달 제거)

### [D] 레벨 시스템 v4.1 (α1~α13) + 성장 상세 (β10)
- α1·α7·α8: 소칭호 7단계(필멸자~주҉신҉) + 색상표 + 주신 무지개 글리치 CSS (`소칭호적용()`)
- α2·α3: Lv.70 하드 캡 + Lv.36~46 ×1.123 — KNOWLEDGE 11 표 14개 지점 수치 검증 통과
- α4: Lv.70 도달 시 현황 탭 「주҉신҉의 경지」 메시지 + 탭 안내
- α5: 창조주 달성 시 소칭호 탭 → 칭호 선택(폐하 ↔ 깨끗한 주신) — **Firestore 신규 필드 `선택칭호` 추가** (13-1 규칙에 따라 보고)
- α6: 소칭호 12px → 14.5px
- α9: 아바타 탭(현황·설정) → 초월자 `{wait!}` / 필멸자 `[권한 부족]`
- α10: [성장의 기록] 조건표 Lv.70 확장 / α12: 업적 팝업 별 ✦✧ 통일 / α13: 등급 체계 강림자까지 확장
- β10: `sg` 화면 구현 — 현재 상태 + 전체 EXP 진행 바 + 소칭호 로드맵 8단계 + 획득 안내

### [E] 토큰 시스템 (β2) + 토큰 UI (β5·β11) + Grok 게이트
- `GROK_활성화 = false` 게이트 — `grok호출()` 첫 줄에서 차단. 봉인 중 토큰 차감 발생 경로 없음
- UI_TEXT(KNOWLEDGE 32 그대로) / `토큰차감()` 차감 공식 전체 / 소진 → 2시간 락 → 전량 충전 / 경고 3단계(75·50·25%)
- 복구: 10연속 정답 +10 구현 / 복습 +30(일 3회)은 복습 플레이 구현 시 연결
- 토큰 UI: 학습 화면 5곳 잔량 바(색 4단계) + 탭 드롭다운(최근 5건) + [? 차감 안내] 팝업 + 락 카운트다운(30초 갱신)
- Firestore `토큰로그` 서브컬렉션 기록 (13-1)
- 질문하기 100회 풀 잔재 전부 제거(KNOWLEDGE 22 폐기 반영) — 설정 행·패널 헤더·학습 화면 미니바 → 토큰 표시로 교체
- 질문하기: 게이트 안내 응답(차감 없음) / 활성화 후: 20 시작·4턴~ +15, 사고전개 ON 시 100·+30
- β7 사고전개: 질문하기 패널 토글(기본 OFF, 열 때마다 리셋) — 구어 교정 쪽은 비용 미확정 보류(21섹션 #13)
- β6 이의있음!: 멀티턴 차감 구조(70 + 턴당 30) + reasoning_note 패널 상단 표시(β3) + 토큰 소진 시 비활성
  — 구 데모의 거짓 「반박의 화신 +1」 연출 제거
- β9 출제 분기: DB 40%/AI 60% + AI문제만 토글 연동 — 봉인 중엔 DB 폴백(세션당 1회 안내 토스트)

### [F] 데모 잔재 정리
- `<title>` → 「코드네임 언어 — 정식 빌드1」
- **상단 데모 네비: 기본 숨김, 개발자 모드 활성화 시에만 노출** (`body.devnav`) — 로그인 우회 차단
- 더미 사용자(홍길동/Lv.15) → 중립 placeholder(학습자/Lv.1/카운터 0)
- "데모 응답입니다"·"데모에서는 동작하지 않습니다" 문구 전부 실동작 또는 정확한 상태 안내로 교체
- 창조주 달성/봉인 시 Firestore 반영 + 개발자 네비 토글 연동

### [G] 문서 갱신
- CLAUDE.md: 파일 구조 실제 반영·진행 현황표·Grok 게이트 경고
- KNOWLEDGE 37섹션 신설(상태표 — 설계 변경 없음, 29·31 미갱신 문제 해소)
- 이 인계 노트 세션 2 기준 재작성

---

## 2. ⚠️ Grok 활성화 절차 (크레딧 구매 후 — 최고 관리자님 승인 필수)

1. xAI Console에서 크레딧 구매 + rate limit(RPM/TPM) 확인 → KNOWLEDGE 21 #12 갱신
2. Cloudflare Workers(`xai-api-llove.hypoqwer.workers.dev`)에 xAI API 키 환경변수 설정 (프론트 노출 금지)
3. Workers가 `{기능, reasoning_effort, ...payload}` POST를 받아 xAI로 중계하는지 확인
4. `index.html`의 `GROK_활성화`를 `true`로 변경 — **이 한 줄이 전체 활성 스위치**
5. 질문하기·이의있음·구어교정·유사문제 실응답 브라우저 검증

## 3. ⚠️ 배포 전 필수 (Firebase 콘솔 — 코드 밖, 미설정 시 DB 전면 개방)

1. **Firestore 보안 규칙** — 본인 문서만 접근:
   ```
   match /users/{userId} {
     allow read, write: if request.auth != null && request.auth.uid == userId;
     match /{document=**} {
       allow read, write: if request.auth != null && request.auth.uid == userId;
     }
   }
   ```
2. **승인 도메인** — Authentication → Settings에 GitHub Pages 도메인 + `localhost` 추가
3. **GitHub Pages** — 저장소 Settings → Pages에서 배포 활성화

## 4. 검증 현황 (세션 2 — 이 환경은 네트워크 개방, 브라우저만 없음)

✅ 완료된 검증:
- `node --check` 문법 + JSON 구조 + EXP 공식 수치(KNOWLEDGE 11 표 14개 지점 일치)
- **CDN 전수 검증**: 눈누 6종·나눔 CDN·Firebase SDK 전부 HTTP 200
- **Cloudflare Workers 배포 확인**: GET → 405 (배포됨·POST 전용 — xAI 호출 없이 확인)
- **jsdom 런타임 스모크 테스트** (로컬 서버 + 실행): 정령왕 JSON 18/7/27건 로드,
  홈 슬라이드 7건 빌드, 유의어 JSON 출제, 구어 출제 카드 표시, EXP획득(+20·연속학습일 1),
  토큰차감(1,500→1,480·로그 기록), GROK_활성화=false 봉인, 가사 10종, 초기화 치명 오류 0건
- **모나 폰트 family 실측 확정**: mona.css에 'Mona'·'Mona-Sans' 없음 → 실제는
  `Mona12` + 한글 `Mona12 Text KR`. FONTS 스택 교정 완료.
  KNOWLEDGE 19섹션 표는 최고 관리자님 승인(2026-06-11)으로 갱신 완료

❗ 남은 미검증 (실 브라우저 필요):
- 구글 로그인 팝업 → 신규/기존 사용자 분기 (Firebase 콘솔 승인 도메인 설정 후에만 가능)
- Firestore 실계정 저장 왕복 · 계정 삭제
- 폰트 실제 렌더링 외관 · 모바일 터치 UX

## 5. 📦 미착수 (세션 2 후반 작업으로 대부분 해소 — 8섹션 참조)

- ~~개발자 모드 오버레이~~ → ✅ 완료 / ~~보관함 서브컬렉션~~ → ✅ 완료 / ~~업적 실계산·퍼펙트 세션~~ → ✅ 완료 / ~~복습 플레이+토큰~~ → ✅ 완료
- 히스토리 서브컬렉션 + β4 히스토리 프롬프트 연동 (Grok 활성화 후 — reasoning_note 저장과 함께)
- [경계를 넘어서]·[질문쟁이] 실동작 (Grok 활성화 후 — 감지·카운트 연결 지점은 구현됨)
- 특정 계정 특혜 (계정 정보 미공개)
- 모드 DB JSON 6종 콘텐츠 채우기 (코드 수정 없이 자동 출제)

## 6. 핵심 코드 위치 (함수명으로 검색할 것)

| 영역 | 검색 키 |
|---|---|
| Grok 게이트 | `GROK_활성화` |
| 데이터 로드 | `예문데이터_로드` / `모드DB_로드` / `구어교정_예문표시` |
| EXP·레벨 | `EXP획득` / `EXP배율` / `expForLevel` / `최대레벨` / `레벨업팝업` |
| 마스터리·학습일 | `마스터리증가` / `학습일갱신` / `모드_마스터리맵` |
| 소칭호 | `소칭호계산` / `소칭호적용` / `소칭호색상표` / `sub-jushin` |
| 토큰 | `토큰차감` / `토큰복구` / `토큰락_체크` / `토큰표시_갱신` / `UI_TEXT` |
| 출제 분기 | `출제_분기` / `출제방식_결정` / `다음문제` |
| 질문하기·사고전개 | `sendAsk` / `사고전개_토글` / `질문턴수` |
| 이의있음 | `submitObj` / `이의턴수` / `현재문제_reasoning_note` / `objNote` |
| 성장 상세 | `렌더_성장상세` / `sgBody` |
| 칭호·커스터마이징 | `칭호선택_모달` / `고급커스터마이징_탭` / `주신의경지_탭` |
| 초기화·개발자 네비 | `학습데이터초기화_1단계` / `갱신_개발자네비_표시` / `devnav` |
| 글꼴 가사 | `글꼴가사` / `가사세트` |

## 7. (참고) 세션 1 완료분 요약

버그 12건 수정(1·2·3·4·5·6·9·A·B·C·D·E, 버그 7은 Firebase 흡수) ·
Firebase Compat 12.13.0 로그인/신규 생성/로그아웃/계정 삭제 ·
`grok호출` helper + 리즈닝 레벨 + Workers 엔드포인트 · Firebase config 실값(`llove-djsdj`)

## 8. 세션 2 후반 — 신규 기능 일괄 구현 (같은 날 연속 작업)

### [H] 보관함 서브컬렉션 동기화 (KNOWLEDGE 13-1)
- 복습 더미 데이터 전면 제거 → `보관함_로드()`가 로그인 시 복습대기열·즐겨찾기·휴지통 로드
- 모든 변경(별 토글·휴지통 이동·복구·영구삭제·비우기)이 Firestore에 즉시 기록 (`보관함_문서추가/삭제/수정`)
- 휴지통 20일 만료분 로드 시 자동 영구 삭제 (이동일시 기준)
- 학습 연동: 퀴즈 오답·플래시카드 헷갈린다/몰랐다·유의어 오답 → 대기열 자동 추가 /
  정답·알았다 → 연속정답수 +1, 3회 시 졸업 / 상한 초과 시 가장 오래된 항목(즐겨찾기 제외) 휴지통 자동 이동 + 자리 5개 이하 경고

### [I] 복습 플레이 + 토큰 충전
- `복습시작()` — 대기열 순차 복습 (뜻 보기 → 기억남/까먹음), 중단 가능
- 완주 시 토큰 +30, 일 3회 한도 (신규 한글 필드: `복습복구횟수`·`복습복구날짜`)

### [J] 업적 진행도 실계산 (KNOWLEDGE 12·14)
- 20종 전체: 카운터 기반 단계 자동 계산 + 단계 상승 시 EXP 지급(일반/히든/단발 표) + 달성 팝업 + `업적로그` 서브컬렉션
- 퍼펙트 세션(5문제 전원 정답 +150), [불굴의 의지](오답·오답·정답), [심연](키워드), [밤의 사색가](00~04시 일1회, 신규 필드 `밤학습수`·`마지막밤기록일`), [지식의 탐닉자](30분), [환경미화원], [첫걸음], [창조주] 전부 실동작
- [질문쟁이] 카운터(신규 필드 `총질문수`)·[경계를 넘어서]는 Grok 활성화 후 실동작
- 업적 화면 상단 요약(획득/진행/미달성) 3/5/12 하드코딩 제거 → 실계산 (발견 오류 수정)

### [K] 개발자 모드 — 오버레이/실DB 분리 완성 (KNOWLEDGE 14 확정 구조)
- 구 간이 스냅샷 롤백 폐기 → `개발자오버레이` 세션 객체 (Firebase 미저장)
- 개발자 패널: 레벨 ±(Lv.120 확장)·EXP·마스터리 증분·업적 풀돌 강제·상한 해제 — 전부 표시 전용
- 실DB는 개발자 모드 중에도 정상 학습으로 계속 기록, 봉인 = 오버레이 제거뿐
- 홈·현황·트리·성장상세가 표시레벨/표시EXP/표시마스터리 합성값 사용

### 검증 (jsdom 런타임)
대기열 추가→3연속 졸업, 복습 완주→토큰 1400→1430(+30), [상식의 탑] 명함 +50 EXP,
[첫걸음] 자동 달성, 요약 1/1/18 정상, 오버레이 Lv.99 표시·실DB 불변·봉인 시 제거,
퍼펙트 세션 +1, [불굴의 의지] 달성 — 전부 통과. node --check 통과.

### 신규 Firestore 한글 필드 (13-1 규칙 보고)
`복습복구횟수` `복습복구날짜` `밤학습수` `마지막밤기록일` `총질문수` `선택칭호`(α5)

---

# 세션 3 인계 (2026-06-14) — 빌드 개선·UX 수정·CI 구축

> 별도 브랜치 `claude/stoic-newton-bcn8rb`에서 작업. 기능 PR과 분리된 UX/CI PR.
> 모든 변경은 jsdom 기능 테스트(`tests/`, 64건) + node --check + htmlhint로 검증함.

## ⚠️ 다음 세션 필수 확인 — 임시 하드코딩 (반드시 교체)
- **sq2 4지선다·역방향 보기용 임시 표본**: `index.html`의 `FC_고사성어_임시보기`·`FC_한자_임시보기`
  (각 5건). 내장 폴백 풀이 각 1건뿐이라 4지선다 오답 보기 3개를 만들 수 없어서 임시로 둔 것.
  → `data/고사성어속담.json`·`data/한자우리말.json`이 채워지면 `sq2_출제풀()`이 DB를 우선
  사용하므로, 이 두 임시 배열은 제거해도 된다. (코드 주석에 "⚠️ 임시 하드코딩" 명시)
- 출제 데이터를 정식으로 채우면 위 임시 표본을 삭제할 것.

## 처리한 항목 (지시서 0~11 + 앱 이름)
- **[0] CI**: `.github/workflows/ci.yml` 신규 — ① JS 문법(`scripts/check-inline-js.mjs`,
  node --check) ② HTML 구조(htmlhint) ③ 배포 점검(`scripts/check-deploy.mjs`)
  ④ 기능 테스트(jsdom, `npm test`). `docs/CI_가이드.md`에 실패 진단·Branch Protection 설명.
  - **Node.js 버전: Node 24** (최신 LTS, 최고 관리자님 지시 2026-06-15). 이전 세션엔 CI 자체가 없었음
    (main에 `.github` 폴더 부재 확인) → 이번이 최초 CI. 앞으로 CI 작업은 Node 24 기준.
- **[1] 폰트 가독성**: `.fo-nm` 9px 대문자→14px 일반·본문색 굵게, 선택 상태 강조.
- **[2] 상업 문구 제거**: 폰트 출처 '상업 사용 가능' 문구·광고 경고문을 사용자 UI에서 제거(주석화).
- **[3] 커스텀 테마**: 스텁 안내모달 → 실제 에디터(4색·32팔레트·미리보기·대비경고·랜덤·이름·10슬롯).
  custom 테마 CSS가 4색에서 나머지 톤을 color-mix로 파생. 신규 키: `plx_커스텀`·`plx_커스텀슬롯`,
  Firestore 필드 `커스텀테마`·`커스텀슬롯`.
- **[4] 커서 깜빡임**: 패널이 transform으로만 숨겨져 입력 포커스가 남던 문제 → `goNav`/`closeAsk`/
  `closeObj`에서 `활성입력_blur()` (CSS 숨김 아님, 근본 수정).
- **[5] 온보딩 바로가기 제거**: 설정 '앱 안내 다시 보기' 행 + 데모 네비 '📖 온보딩' 버튼 제거.
  (첫 실행 온보딩 플로우·`showOb()` 함수는 유지)
- **[6] AI 채팅**: 하단 시트를 반투명 리퀴드 글래스(backdrop-filter)로 전환, 배경 딤 완화.
- **[7] 학습 모드(최우선)**: ① sq5(구어 교정) 진입 분기 누락 추가 ② sq2 5방식(4지선다/역방향/
  뜻 직접 서술/유의어 변별/플래시카드) 실제 분기. 정답 처리는 기존 `selAns` 재사용.
- **[8] 음성 입력**: Permissions-Policy 마이크 차단 제거 + Web Speech API(`ko-KR`) 구현.
  미지원 브라우저(Firefox 등) 안내. 채점은 현 단계 불필요(Grok 봉인 유지).
- **[9] 가독성/글자 크기**: 설정>외관에 글자 크기 8단계(80~200%, root zoom) 추가, 저장/복원.
  신규 키 `plx_글자배율`, Firestore `글자배율`. 학습설정·복습탭 등 작은 글자 상향.
- **[10] 복습 UX**: 복습 '하기'를 전용 화면 `srp`로 분리(좁은 복습칸 인라인 → 전용 화면, 바텀 네비 숨김).
- **앱 이름**: 사용자 노출 명칭 'Llove'로 통일(타이틀·로그인 로고·온보딩) + OG/메타 태그.

## 변하지 않은 절대 고정
- **Grok 게이트(`GROK_활성화=false`) 그대로 봉인.** 음성 입력은 Grok과 무관(브라우저 API).
- 플래시카드 ①~⑥ 번호·레이블, 바텀 네비 독립 위치 유지.

## 신규 localStorage 키 / Firestore 필드
- localStorage: `plx_글자배율` `plx_커스텀` `plx_커스텀슬롯`
- Firestore: `글자배율` `커스텀테마` `커스텀슬롯`

## 테스트
`npm test` (jsdom 64건) — 학습11·커서5·음성10·글자9·복습11·커스텀18 전부 통과.
브라우저 실동작(특히 음성 인식·zoom·글래스 blur)은 실제 모바일에서 추가 확인 권장.

---

## [세션 4] 추가기능: 콜롬비나 음성 생성(TTS) 봉인 골격 — 완료
> 브랜치 `claude/stoic-newton-bcn8rb`. 상세: `docs/음성생성_봉인골격.md`
> (§7 「밖에서 내 PC를 서버로 쓰기」 비전문가용 운영 가이드 추가 — RTX4060 1순위·IoT/WOL 깨우기·
>  서버 자동실행·ngrok 무료 고정주소·보안키(=비밀번호, AI토큰 무관)·단어 캐싱·무료GPU 현실).

- **목적**: 로컬 So-VITS 콜롬비나 음성 TTS를 Llove로. 끝말잇기에서 단어 말하기용
  (끝말잇기 게임 자체는 아직 없음 — 다른 세션 완성 후 제공. 도착 시 `음성생성호출(단어)` 연결).
- **구조**: 파이썬 모델은 외부 GPU 서버에서 돎. 브라우저는 텍스트 보내고 음성만 받아 재생.
  RVC 실시간 변조 제외(TTS만). 서버=본인 PC+터널 또는 유료 클라우드 GPU(AWS 프리티어엔 GPU 없음).
- **구현(index.html, Grok 봉인 패턴 복제)**:
  `const 음성생성_활성화=false`(⚠️ 승인 없이 true 금지) / `음성생성호출()`(봉인 시 fetch 없이 null) /
  `음성재생()`(new Audio, 프로젝트 최초 오디오 재생) / `음성생성_로딩()`(회전 금지 펄스) /
  창조주 게이트 `#음성설정Row`·`갱신_음성설정_UI()`·`음성설정_탭()` / 테스트 벤치 `#음성생성Bg`·`음성_테스트생성()` /
  엔드포인트 `plx_음성엔드포인트`+Firestore `음성엔드포인트`(코드에 주소 비저장).
- **`.gitignore`**: 모델·음성 파일 커밋 차단(`*.pth`/`*.onnx`/`*.wav`/`/models/` 등). 저작권: 호요버스/성우.
- **나중**: 파이썬 HTTP API 서버 + (권장) Worker 프록시 + 창조주가 주소 입력 → 검증 후 승인받아 플래그 true.

## 신규 localStorage 키 / Firestore 필드 (세션 4)
- localStorage: `plx_음성엔드포인트`
- Firestore: `음성엔드포인트`

## 테스트 (세션 4 갱신)
`npm test` 전체 통과 — 기존 묶음 + `test-voice.cjs` 21건(봉인 차단·창조주 게이트·엔드포인트 영속·로딩·재생경로) 추가.
실제 음성 종단 검증은 서버 준비 후 브라우저에서 수동.

## 변하지 않은 절대 고정 (세션 4)
- **`음성생성_활성화=false` 봉인 유지** — Grok과 동일, 승인 없이 true 금지.
- `GROK_활성화=false` 그대로. 모델·음성 파일 레포 미포함 원칙.

---

## [세션 5] 실사용 검토 버그 수정 — 전체 완료
> 브랜치 `claude/stoic-newton-bcn8rb`. 계획: `docs/수정계획_세션5.md`(✅ 완료 처리).
> 검증: node --check·htmlhint·배포점검 + jsdom 130건(신규 test-fixes-세션5.cjs 40건) 전부 통과.

### 최고 관리자님 발견 9건
1. **화면/글자 크기 분리** — `setFontScale`=화면 크기(zoom, `plx_화면배율`·Firestore `화면배율`) /
   신설 `set글자크기`=읽기 텍스트 전용(`--글자배율` CSS 변수 + calc 오버라이드 블록, `plx_글자배율` 재사용,
   80~150% 6단계). 설정 화면에 두 행 분리. 기존 테스트 개편(16건).
2. **커스텀 테마 이름 라벨** — `커스텀칩_라벨갱신()`: 이름 있으면 「🎨 (이름)」, 없으면 「🎨 커스텀」.
   저장·복원 시점 동기화.
3. **사고전개/채팅 가시성** — `.obj-note`·`.ask-msg.user` 글자색 `--txt2`→`--txt` (+obj-note 11→12px).
4. **칭호 가시성** — 소칭호색상표 저명도 색 명도 상향(시련·최고신 등) + `.status-sub-title`·성장상세
   인라인에 text-shadow 외곽선.
5. **강림자 표시 통일** — `렌더_성장상세()`에 `등급정보()` 추가, 현황과 동일한 「등급: 강림자」 표시.
   (강림자=Lv.70 정식 등급(α13). 계정 특혜·임의 추가 아님 — YYYYYNNY는 게스트 진입 이스터에그일 뿐)
6. **창조주 퀘스트 반복 — 방식 A(완전 차단)** — sendAsk 창조주 분기에 `사용자.창조주달성` 체크,
   「이미 창조주입니다」 안내만. B(재감상)로 바꾸려면 그 if 블록 제거+멱등 처리.
7. **학습 설정 실반영+영속** — `setLsetMode`가 sq1/sq3/sq4_input도 즉시 재출제. **직접입력 실구현**:
   `직접입력_HTML/규격/제출`(입력→느슨 비교→selAns 동일 후처리, 1회 잠금) + 아재 `아재_직접제출`.
   `plx_학습설정` 저장 + `학습설정_UI동기화()` 복원.
8. **패널 애니** — rotate 제거(규칙 위반이기도 했음)→▼/▲ 글자 교체, `.lset-body`에 opacity·padding 동시 전환.
9. **커서 깜빡임** — `closeNmModal`·`음성생성패널_닫기`에 `활성입력_blur()` 추가, 직접입력 제출 후 blur.

### 추가 발굴 수정
- 플래시카드 판정 잠금 미해제(다음 카드 먹통) — `initFlashcard`에서 data-판정완료 제거.
- 보관함 로드 레이스 — 로드 전 로컬(`로컬*`) 항목 보존·병합 + Firestore 기록.
- 게스트 복습 데이터 소실 — `plx_게스트보관함` 폴백 저장/복원(`게스트로그인` 시 복원).
- 구어 교정 EXP 재획득 — `plx_구어완료_<uid|게스트>` 영속(`보관함_로드`/게스트 진입 시 복원).
- revealDad 중복 EXP — `아재정답공개됨` 1회 잠금.
- 마스터리 매핑 실패 시 콘솔 경고 / `.has-bnav` 72→96px / 토스트 max-width·줄바꿈 / `선택칭호` 초기값·리셋 포함.

### 신규 localStorage 키 (세션 5)
`plx_화면배율` `plx_학습설정` `plx_게스트보관함` `plx_구어완료_*` / Firestore 필드 `화면배율`

---

## [세션 6] 실기기 2차 검토 수정 14건 + 신규 기능 — 전체 완료
> 브랜치 `claude/stoic-newton-bcn8rb`. 검증: 정적 3종 + jsdom 172건(신규 세션6 42건) 전부 통과.

### 피드백 14건 처리
1. **커서 깜빡임(근본)** — body 전역 `user-select:none`+`caret-color:transparent`, input/textarea만 예외. div 텍스트 탭 캐럿 차단.
2. **사고전개** — thinkToggle 가시성 상향(--txt·11px) + `.ask-think` 렌더 블록 신설(Grok 활성화 시 res.사고전개 표시) + 게이트 안내 병기.
3. **창조주 중복 문구** — 지정 문구로 교체: 「알현 / 시스템의 창조자이자 최고 권력자이신, 드높으신 폐하를 알현합니다.」
4. **채팅 기록(신규)** — 세션 단위(카테고리 이동·새 대화·메시지 100개 도달 시 마감), 최근 30세션 보존.
   로그인=Firestore `채팅기록` 서브컬렉션 / 게스트=`plx_채팅기록`. 설정 「채팅 내역」 뷰어(목록→전문) +
   ask 패널 「🆕 새 대화」 버튼. 카테고리 이동 시 채팅창 비우고 기록 보관(확정 사양).
5. **AI 지침(신규)** — 설정 「AI 지침」: 통합 지침 500자(공백 포함), 초과 시 빨간 음수 카운터,
   저장 시 초과면 「저장 실패」+원인 팝업. `plx_AI지침`+Firestore `AI지침`, `grok호출` 공통 body `지침` 필드로 전 기능 전달.
6. **학습설정 기본 on 누락** — `동기화_학습설정_버튼`을 onclick 파싱 기반 행-정확 동기화로 재작성(sq4 입력방식 행 해소) + 초기 로드에서 항상 동기화.
7. **랜덤 모드(신규)** — 홈 「🎲 랜덤」 카드 + `랜덤학습()`(7개 모드 임의 goLearn).
8. **학습 모드 강조** — .sec-t 12px·--txt / .mc 테두리·액센트바 상시 강조 / .mc-name 15px·--accl.
9. **개발자 네비** — 자동 노출 제거: `개발자모드 && 개발자네비표시`(기본 false)일 때만. 개발자 패널에 「상단 바로가기」 토글.
10. **글자 크기** — 로직 정상(원인=설정 화면에 적용 대상 없음). 실시간 미리보기 문장 추가 + 인라인 font-size 2곳 배율 적용.
11. **프로필 개방(신규)** — 전원: 이모지 12종+`assets/프로필/목록.json` 이미지 프리셋. 직접 업로드만 Lv.16 게이트
    (canvas 128px 리사이즈→dataURL→Firestore). 아바타 4곳 이미지/<img> 렌더 지원 + 홈 아바타 탭 활성화.
    `assets/프로필/` 신설(출처.md=출처 표기+「저작권자가 삭제를 요청하면 반드시 삭제합니다」 / 목록.json 빈 목록).
    ※ 晚餐歌 아트워크 등 실제 이미지 파일은 최고 관리자님이 폴더에 추가+목록 등록 시 자동 노출.
12. **모달 드래그-아웃 방지** — 포인터다운 추적+`배경클릭_닫기()` — 배경 9곳 교체(누름·뗌 모두 배경일 때만 닫힘).
13. **강림자 사라짐 = 정상** — 개발자 오버레이가 세션 전용(새로고침 시 소멸)이라 실레벨로 복귀한 것.
    실제 Lv.70 달성 시엔 유지됨. 패널에 안내 문구 추가. 명칭은 최고 관리자님 보류 결정대로 유지.
14. **신규 발굴 수정** — ①구어 '자유입력' EXP 영구 잠금(세션5 회귀) → 영속 제외 ②게스트 보관함 로그인 승계
    ③보관함 병합 재-add 제거(중복 문서 방지) ④모바일 뒤로가기 pushState/popstate 지원.

### 신규 localStorage 키 / Firestore (세션 6)
- localStorage: `plx_채팅기록` `plx_AI지침` / Firestore: 서브컬렉션 `채팅기록`, 필드 `AI지침`·`개발자네비표시`
- 관찰 항목(미수정): root zoom의 iOS 사파리 fixed 요소 상호작용 — 실기기 확인 후 필요 시 개선.

---

## [세션 7] 실기기 3차 피드백 16건 + 신규 발굴 — 전체 완료
> 브랜치 `claude/stoic-newton-bcn8rb`. 검증: 정적 3종 + jsdom 222건(신규 세션7 50건) 전부 통과.

1. **봉인=창조주 업적 완전 리셋** — 업적진행도['창조주'] 삭제+ACH_DATA 잠금+업적로그 문서 삭제+안내 문구.
   (지급된 +2000 EXP는 「정당한 기록 유지」 설계대로 회수 안 함 — 정책 변경 원하면 별도 지시)
2. **실험실(위험 구역)** — 티저형: 목록 노출, 탭 시 「준비 중… 기다려 주세요(개발자 모드 먼저 체험)」.
   개발자 모드+연결 가능 항목(음성 생성)은 실제 열림. 목록: 실험실_목록 배열.
3. **전역 글씨 +10%** — scripts/폰트10퍼확대.mjs 로 <style> font-size 216건 ×1.1(반올림).
   제외: .nv-btn/#g-bnav/.onum/.rv-idx/.act-btn/.toast. calc 기준값도 상향 → 글자 크기 100%=새 기본.
4. **음성 저장/지우기 문구 분리** — 저장됨/입력 없음/삭제됨 3분기.
5. **복습** — 졸업 3연속→**1회**(판정·점·카운터·문구). srp에 lset 패널: 방식(카드/4지선다/직접입력)+순서(등록순/섞기).
   4지선다 오답=대기열 다른 뜻(부족 시 카드 폴백). 학습설정.srp/srp_순서.
6. **아재 '달' 문제** — 《사이버펑크: 엣지러너》로 교체. 표시 a='데이비드 (또는 루시)',
   내부 허용 배열 11종(이름/성+이름 정답, 성만 오답). 아재_직접제출·직접입력 허용 배열 지원.
7. **방식 통일·확장** — '선택지'→'4지선다'(저장값 마이그레이션: 학습설정_마이그레이션).
   sq1·sq3에 플래시카드/역방향 추가(퀴즈_플래시렌더/퀴즈_역방향렌더 — 문항<2면 폴백).
   sq4: 구 '선택지'(탭→공개)를 '플래시카드'로 정직화 + 진짜 4지선다 신설(아재_오답표본 임시 — DB 채우면 제거).
   맞춤법 보기 4개화('됀지' 추가).
8. **랜덤 ⚙** — 모드별 가중치 0(제외)/1/2/3, plx_랜덤설정+Firestore. 전부 제외 시 안내.
9. **학습 설정 헤더 강조** — 배경 톤+액센트 바+기본 글자색.
10. **글자 크기 적용 범위** — [학습만/전체] 토글(plx_글자범위), 전체 시 html[data-글자범위="전체"]로 UI 텍스트 확장.
11. **프로필 팝업** — 관리 방법(폴더·목록.json) 문구를 사용자 화면에서 제거(코드 주석으로만). 업로드 Lv.16 정책 유지 확정.
12. **현황** — 이름 24px + **배너**(그라디언트 프리셋 6종 전원/이미지 프리셋 assets/배너/전원/업로드 Lv.16, 640×180).
    필드 `배너이미지`. assets/배너/{출처.md,목록.json} 신설.
13. **마스터리 증분 답변** — 표시 전용 오버레이(실DB 무관·비저장·새로고침 소멸)라 상한 불필요 = 의도된 설계.
14·15. **게스트 정책 확인** — 진입은 'yyyyynny' 키 시퀀스 유일, 일반 사용자는 구글 로그인 외 경로 없음(코드 재확인).
    게스트 localStorage 저장은 백도어 테스트 편의일 뿐 → 정책 유지.
16. **신규 발굴 수정** — 뒤로가기 학습 화면 goLearn 재진입(EXP 오집계 해소)/네비 간 replaceState(히스토리 폭증 방지)/
    .ask-msg 복사 허용/창조주 진행 중 배경 탭 무시/채팅기록 30개 초과 문서 로드 시 정리.
    (인계) Grok 활성화 시 res.답변 innerHTML sanitize 필요.

### 신규 localStorage/Firestore (세션 7)
`plx_랜덤설정` `plx_글자범위` / Firestore 필드 `랜덤설정`·`글자범위`·`배너이미지`

---

## [세션 8] 첫 화면 여백 반응형 + 백도어 터치 제스처 — 완료
> 브랜치 `claude/stoic-newton-bcn8rb`. 검증: 정적 3종 + jsdom 239건(신규 세션8 17건) 전부 통과.

1. **첫 화면 여백 반응형** — 원인: `#sl`·`#sn`·온보딩만 "고정 max-width(340/300/320px)+가운데 정렬"
   패턴이라, 화면 폭이 그 값+패딩(약 396~404px)을 넘는 태블릿·폴더블·대화면폰에서 잉여 여백이
   폭에 비례해 계속 벌어짐(좁은 기기는 캡이 아예 안 걸려 여백=패딩뿐) → "기기마다 다르다"로 체감.
   수정: `.lg-content`·`.ob-sub`·`.ob-feats`·`.sn-content`(신규, 인라인 통합) 전부 `max-width:min(Npx, 100% - Mpx)`로
   교체, `#sl`/`.ob-slide` 좌우 패딩을 `clamp()`+`env(safe-area-inset-*)`로(노치·펀치홀 대응).
   viewport 메타에 `viewport-fit=cover` 추가.
2. **백도어 모바일 대응** — 안드로이드는 포커스된 입력창이 없으면 화면 키보드 자체를 안 띄워
   기존 'yyyyynny' 키 시퀀스가 모바일에서 원천 불가(KiKi 우회도 권한·루팅·ADB 문제로 비현실적).
   → 로그인 화면 로고(`lg-icon-wrap`, 순수 장식·오탭 위험 낮음)를 3초 내 8회 연속 탭하면
   `게스트로그인()` 호출(`백도어_로고탭()`). **키보드 시퀀스는 그대로 유지**(PC 편의) — 서로 다른
   트리거가 같은 실행부를 호출하는 구조.

### 참고
- 두 기능 모두 코드 변경 최소, 신규 localStorage/Firestore 필드 없음.

---

## [세션 9] 모바일 위화감 3건 — 온보딩·아이콘·학습 콘텐츠 맥락화 — 완료
> 브랜치 `claude/stoic-newton-bcn8rb`. 검증: 정적 3종 + jsdom 259건(신규 세션9 20건) 전부 통과.
> 계기: "모바일로 보니 거부감 든다 · 이모티콘도 구려 · 학습이 단어 위주(모의고사·대화는 맥락인데)".

1. **온보딩 하단 잔여물** — 마지막 슬라이드에서 이전/다음 바(`.ob-nav`)가 개별 버튼(`opacity:0`)만
   투명해지고 자리는 남아, 이미 있는 "학습 시작하기" 버튼과 두 겹으로 보이던 문제. `setObSlide()`에서
   마지막 슬라이드면 `.ob-nav` 전체를 `display:none`으로 완전히 숨기도록 수정.
2. **아이콘 통일** — 43곳 조사 결과 크기 9종 제각각 확인. 히어로(44px: `.ob-icon`·`.lg-icon-wrap`,
   기존 55/40)·목록(26px: `.ob-fi`, 기존 24)·소형(20px: `.sri`, 그대로) 3단계로 수렴.
   **구글 로그인 버튼의 가짜 파란 원 이모지(🔵) → 실제 구글 4색 "G" 로고 SVG로 교체**(제일 눈에
   띄던 "가짜스러움"의 원인).
3. **학습 콘텐츠 맥락화(핵심)** — 조사로 확인: 7모드 중 5개(71%)가 순수 단어-뜻 암기, 맥락형은
   구어 교정 1개뿐. 단어형 모드 DB는 전부 빈 파일인데, 실제 채워진 콘텐츠(정령왕_통합_v2.json 52건)는
   오히려 전부 맥락형이었음 — 기본 노출이 거꾸로였음.
   - **sq2(고사성어·속담/한자·우리말) 기본값을 '4지선다'→'유의어 변별'(예문 빈칸형)로 전환.**
   - **유의어 변별 로직을 공용 엔진(`예문형_렌더`/`예문형_선택`/`예문형_다음`)으로 일반화** —
     정답·근사·오답 3단계 판정+EXP·복습·마스터리 연동을 그대로 유지하며 다른 화면도 재사용 가능하게.
   - **sq1(상식·어원/세계사·신화)에 「예문형」 옵션 신설**(선택 사항, 기본은 4지선다 유지 — 데이터
     자산이 아직 얇아 강제 전환은 보류). 임시 표본 각 2건(`예문형_상식어원`/`예문형_세계사신화`,
     세션3 임시보기와 동일 관리 방침 — data/*.json 채워지면 교체).

### 참고
- 새 localStorage/Firestore 필드 없음(기존 `plx_학습설정` 값만 갱신).
- 이번엔 처리 안 함(보류): 전체 커스텀 SVG 아이콘 세트 교체, 맞춤법/아재개그의 맥락화 확장,
  구어 교정 인프라의 맞춤법 이식 — 필요 시 별도 세션.

---

## [세션 10] 4차 피드백 5건 처리 — 부분 완료(1·2·4건) + 밸런스 조정 + 문서 동기화
> 브랜치 `claude/session-10-feedback-plan-ixpl91`. 사전에 AskUserQuestion으로 세부 범위 확인 후 진행.
> 검증: `node --check`(인라인 JS 문법) 통과. `npm test`는 이 노트 작성 시점 기준 별도 확인 필요(아래 검증 항목 참조).

### 처리 완료
1. **sq2 옵션명 리네이밍만** — "유의어 변별" → "예문형"으로 명칭 통일(로직·데이터·기본값 변경 없음).
   `setLsetMode` 버튼, `학습설정.sq2` 기본값, 분기 조건(`sq2_출제_렌더`), 이의있음 타이틀, 홈 카드 설명
   전부 갱신. **sq1은 이번 세션에 손대지 않음**(4지선다 기본값 그대로, 데이터 채우는 것도 보류).
   구버전 저장값(`유의어 변별`) → `학습설정_마이그레이션()`에 승계 로직 추가(기존 사용자 설정 보존).
2. **계정 삭제 안내 문구** — "(2중 확인)" 사용자 노출 텍스트 제거. 실제 2단계 확인 로직·내부 주석은 그대로 유지.
3. **EXP·업적 밸런스 디버프(×1.45, 체감 40~50% 느리게)** —
   - `expForLevel()`: 기본 공식 결과에 ×1.45 추가 적용(하급신 구간 ×1.123은 그 위에 그대로 유지).
     누적 총 필요 EXP 약 77,000 → 약 112,000.
   - 카운터 기반 업적(레벨 기반 [성장의 기록] 제외) 전체 임계값 ×1.45 — `ACH_DATA`(화면 표시용 rows)와
     `업적_정의`(실계산용 임계) **두 곳 모두** 갱신(하나만 고치면 표시-판정 불일치 발생).
   - **`EXP배율()`([꾸준한 발걸음] 연속학습일 EXP 배율 함수)도 함께 갱신** — 원래 업적_정의.steady와
     동일한 임계값을 공유하는 설계였는데(KNOWLEDGE 14 "단계 달성 시 배율 혜택"), steady만 고치고 이 함수를
     놓치면 업적 단계와 배율 단계가 어긋나는 회귀가 생겼을 뻔함. 발견해서 같이 수정함.
   - 개별 EXP 지급량(퀴즈 +20, 구어교정 +60 등)은 그대로 — 진행 속도는 "요구량" 쪽에서만 중앙 조정.
4. **모드 카드 sq1/sq2 계열 색 구분** — 신규 CSS 변수 없이 기존 테마 변수 재사용:
   상식·어원·세계사·신화(sq1 계열) → `--inf`, 고사성어·속담·한자·우리말(sq2 계열) → `--warn`.
   `.mc-g1`/`.mc-g2` 클래스로 좌측 액센트바+테두리 상시 표시(호버 없이도 구분됨). 나머지 4개 카드(아재개그·
   맞춤법·구어 교정·랜덤)는 기존 `--acc` 그대로.
5. **문서 동기화** — `언어_KNOWLEDGE_v5.md` 학습 모드 표(§2·§2-2·§3의 sq1/sq2 기본값·"선택지"→"4지선다"
   잔존 표기·sq1 예문형 옵션 누락 등 세션7~9 변경 미반영분) + §11(레벨 공식·표)·§14(업적 임계표 전체) 밸런스
   디버프 반영. 코드-문서 수치 불일치 해소.

### 보류(다음 세션 — 사용자가 별도로 플랜 모드에서 논의 예정)
- **항목3(근본 질문)**: "책을 안 읽고 문장력·문해력을 높일 수 있는가" — 이번 세션엔 논의도 구현도 진행
  안 함. 사용자 확인: "일단 보류하고 지금 하는 작업 후 오푸스 플랜으로 가서 논의할 예정."
- **항목5-①**: "긴 글" 모드 신설 — 항목3의 방향이 정해지지 않아 함께 보류(신설한다면 항목3 논의 결과에
  따라 구체 형태가 결정되므로 지금 만들면 되돌릴 위험).
- sq1(상식·어원/세계사·신화) 기본값 예문형 전환·데이터 채우기: 이번 세션 범위 밖(사용자가 "sq1은
  이번엔 적용 안 함"으로 확정).

### 신규 코드 지점 (다음 세션 검색용)
- `.mc-g1` / `.mc-g2` (모드 카드 그룹 CSS)
- `EXP배율` 임계값 변경 지점 — [꾸준한 발걸음] 관련 코드 수정 시 `업적_정의.steady`와 함께 반드시 동기화할 것

### 검증 남은 것
- `npm test`(jsdom 전체 스위트) 실행 확인 — 임계값·EXP 공식 하드코딩 assert 없음을 사전 조사로 확인했으나
  실제 실행 결과는 재확인 필요.
- htmlhint 정적 검사.
- 브라우저 실동작: sq2 예문형 진입, 계정삭제 문구, 모드 카드 색상 구분, 레벨업/업적 체감 속도.

---

## [세션 10-b] 항목3 오푸스 논의 → 문해력 1탄 "문단 빈칸 추론"(sq6) 신설
> 같은 세션 내 이어서 진행(오푸스 플랜 모드). 세션10에서 보류했던 항목3·항목5-①에 대한 결론과 구현.
> 검증: `node --check`(인라인 JS)·`npm test`(전체 jsdom, 신규 4건 포함)·htmlhint 전부 통과.

### 논의 결론
"책을 안 읽고 문장력·문해력을 높일 수 있는가"에 대한 답: 부분적으로 가능하나 현재 방식엔 천장이 있음.
지금 앱은 어휘 **재인**만 훈련하는데, 문해력에는 그걸로 못 채우는 것 — **문장을 가로질러 맥락을 붙드는
힘**(작업기억·추론·응집성 파악)이 있음. 해법은 "책 읽으라 안내"가 아니라 **연결된 지문(discourse)을
마이크로러닝 형식으로 앱 안에 넣는 것**.

**모드 후보 5가지(A~E) 전체 표** — 최고 관리자님 요청으로 세션 전체 대화 로그에서 원 논의 원문을
찾아 복원(노트엔 그동안 A/B/C만 남아있었음):

| 방향 | 훈련하는 근육 | Grok 필요? | 비용/난이도 |
|---|---|---|---|
| A. 지문 독해 Q&A(200~400자+주제/추론/세부 문제) | 문해력 정통 | 출제·채점 둘 다 필요 | 무거움 |
| B. 요약/재구성(지문 읽고 요약 제출) | 문해력+문장력 동시 | 채점 필요(주관식) | 무거움 |
| **C. 문단 빈칸 추론**(문단 속 빈칸, 맥락에 맞는 말) | 맥락 유지 | 불필요 — 예문형 엔진 확장 | 가벼움 |
| **D. 문장 배열**(뒤섞은 문장 재배열) | 응집성·연결 논리 | 불필요 — 순수 DB | 가벼움 |
| E. 문장 고쳐쓰기(구어교정 확장) | 문장력 | 채점 필요 | 중간 |

당시 Opus의 추천은 **C+D를 함께**("가벼운 것부터, Grok 봉인 상태에서도 되는 것" — 둘 다 Grok 불필요)였으나,
최고 관리자님은 이번 세션 범위를 **C안(문단 빈칸 추론)만** 확정 — Grok 봉인 상태에서도 예문형 엔진
재사용으로 바로 실동작 가능하고, "긴 글"과 모바일 UX 충돌을 미니 지문(2~4문장)으로 타협. **D안(문장
배열)은 C와 함께 추천됐지만 아직 미착수 — Grok 없이 순수 DB로 바로 만들 수 있는 상태라 다음 문해력
후속 라운드의 유력 후보.** A안(지문 독해 Q&A)·B안(요약 채점)·E안(문장 고쳐쓰기)은 채점/출제에 Grok이
필요해 해제 후 2단계로 보류.

### 처리 완료
- **신규 화면 sq6 "문단 빈칸 추론"** — 홈 카드 1개 추가, sq1 골격 복제하되 방식 옵션이 없어
  `.lset-panel` 생략(학습설정/setLsetMode/마이그레이션 배선 전혀 불필요).
- **엔진은 한 줄도 안 건드림** — `예문형_렌더`/`예문형_선택`(sq1·sq2가 이미 쓰는 그 함수)을 컨테이너
  id(`sq6Body`)와 데이터풀만 바꿔 그대로 재사용. 이 모드의 정체성은 **코드가 아니라 데이터**(지문이
  2~4문장이라 앞 문장을 종합해야 빈칸이 풀리도록 설계)에 있음.
- 배선: `SHOW_NAV`·`NAV_MAP`에 `sq6` 등록(누락 시 하단 네비 사라짐), `goLearn` 분기 추가,
  `모드_마스터리맵`에 `'문단 빈칸 추론':'문해력학습수'`(전용 카운터, 기존 카운터에 안 얹음), `사용자`
  기본 객체 3곳에 `문해력학습수:0` 초기화.
- 임시 데이터풀 3건(정적) + `예문데이터_로드`에 `d.문단_빈칸` JSON 훅(키 없으면 무해, 정적 폴백 유지).
- `tests/test-learn.cjs`에 sq6 케이스 4건(보기 렌더·이의있음 버튼 없음·EXP 획득·마스터리 카운터 증가) 추가.
- `언어_KNOWLEDGE_v5.md` §2(학습 모드 7→8개 표+각주)·§3(문단 빈칸 추론 설명 문단) 갱신.

### 보류(다음 세션 또는 Grok 해제 후)
- **전용 업적** — `문해력학습수` 카운터는 집계만 되고 있고, 매핑되는 업적은 아직 없음(콘텐츠 채운 뒤 정의).
- **`data/문단빈칸.json`** — 아직 안 만듦. 정적 임시 표본 3건으로 오프라인 동작 중. 채우면 자동 교체.
- **A안(지문 독해 Q&A)·B안(요약 채점)·E안(문장 고쳐쓰기)** — 채점/출제에 Grok 필요, 해제 후 2단계.
- **D안(문장 배열)** — Grok 불필요(순수 DB)라 C와 함께 바로 만들 수 있었지만 이번 세션 범위엔 안 넣음.
  문해력 후속 라운드에서 우선순위 높은 후보(다음에 "문해력 2탄"을 하게 되면 가장 먼저 검토할 것).
- **알려진 코스메틱**: `예문형_선택`이 EXP 사유 `'예문형 정답'`·복습 태그 `'예문형'`을 하드코딩 →
  sq6도 같은 라벨 사용(기능은 정상, 라벨만 부정확). 공용 함수라 지금 손대면 sq1/sq2까지 영향 받음 →
  라벨 파라미터화는 후속 세션.

### 신규 코드 지점 (다음 세션 검색용)
- `sq6` 화면 / `문단빈칸_렌더` / `문단빈칸풀` / `문해력학습수` (세션10-c에서 `독해_렌더`/`지문독해풀`로 대체됨 — 아래 참조)

---

## [세션 10-c] 실기기 5차 피드백 6건 — 채팅버그·카드색·배너 재설계·sq6 독해 전환
> 세션10-b 직후 실기기 피드백. 오푸스 논의 후 소넷으로 전환해 구현(설계 판단 무거운 항목3만 오푸스 사용,
> 이후 코드 작업은 소넷 — 최고 관리자님 지시). 순서: ①채팅버그→②카드색→③배너→④sq6 독해 전환.
> 검증: `node --check`·`npm test`(전체+신규 케이스)·htmlhint 전부 통과.

### ① AI 채팅 저장 버그 (항목5)
원인: 대화는 `현재채팅세션`(메모리)에만 쌓이고 `채팅세션_마감`(카테고리 이동·새 대화·100상한)에서만
`plx_채팅기록`/Firestore로 flush됨 → 앱 종료·새로고침·백그라운드 시 마감 없이 통째 소실.
수정: `document.visibilitychange`(hidden 시)·`window.pagehide`에 `채팅세션_마감('앱 이탈/백그라운드')`
강제 호출 추가(index.html, 채팅세션_마감 함수 바로 아래). 모바일에서 pagehide가 안 오는 경우가 있어
visibilitychange를 1차 신호로 둠. 부작용: 탭을 잠깐 백그라운드로 보내도 대화가 즉시 마감·보관되고
채팅창이 인사말로 리셋됨(카테고리 이동과 동일 취급) — 데이터 유실 방지를 우선한 의도된 트레이드오프.

### ② 모드 카드 재배열·색 대비 강화·s7 중복 정리 (항목2)
- 카드 순서를 같은 계열 인접으로 재배치: 상식·어원·세계사·신화(g1) → 고사성어·속담·한자·우리말(g2) →
  지문 독해(g3, 신규 그룹) → 아재개그·맞춤법·구어 교정(무그룹) → 랜덤.
- `s1`~`s9` 애니메이션 딜레이 클래스를 새 순서대로 재부여(기존 `s7`이 구어 교정·랜덤에 중복 부여돼
  있던 버그 해소, `.s9` 규칙 신규 추가).
- 고서 테마 등에서 `--warn`(주황)이 `--acc`(황갈)와 구분 안 되던 문제 → `color-mix` 혼합비율 40%→65%,
  좌측 액센트 바 상시 5px(기존 4px, 호버 시 6px는 유지)로 확대.
- 지문 독해 전용 색 `.mc-g3` 신설(`--ok` 재사용, 신규 CSS 변수 없음).
- 보류: 색맹 접근성을 위한 텍스트/아이콘 그룹 라벨은 이번엔 추가 안 함(저비용 판단 유보 — 색+두께+
  인접배치 3중 신호로 우선 대응).

### ③ 배너 재설계 — 현황·설정 통합 (항목4)
- 배너가 이름칸(`.st-prof`/`.set-prof`)과 분리된 독립 박스(72px)였던 걸 **이름칸 자체의 배경**으로 합침.
  신규 공용 클래스 `.prof-banner`(배경 레이어 `.banner-layer` + 가독성용 어두운 스크림 `::after` +
  콘텐츠 z-index 상승). 카드 높이 72px→136px로 확대해 배너 실효 면적 확보.
  텍스트(`.st-name`/`.set-nm`/`.st-ttl`/`.set-ttl`/`.set-em`)는 스크림 위에서 흰색+그림자로 가독성 확보.
- **설정 탭에도 동일 배너 적용** — 신규 id `settingsBanner` 추가, `배너_적용선택()`이 `statusBanner`·
  `settingsBanner` 양쪽에 동시 적용하도록 수정. `afterNav('sse')`에 `배너_적용(settingsBanner, ...)` 추가.
- 저장은 원래도 Firestore(`사용자.배너이미지`)였음 — 로컬스토리지 아니었음(요청 배경의 오해 정정).
- 업로드 크롭 비율을 640×180(3.55:1)→640×260(2.46:1)로 조정 — 새 카드 표시 비율에 맞춰 이중 크롭으로
  이미지가 과도하게 잘리는 문제 완화. `id="statusBanner"`는 유지해 세션7 배너 테스트 하위호환.

### ④ sq6 "지문 독해" 전환 + 예문형 넛지 (항목3·항목1)
- **"문단 빈칸 추론이 예문형의 긴 버전일 뿐"이라는 지적을 그대로 수용** — 빈칸 채우기(단어 고르기)는
  지문이 길어도 어휘 과제일 뿐이라는 판단 하에, 예문형 엔진(`예문형_렌더`/`예문형_선택`) 의존을 완전히
  버리고 신규 엔진 `독해_렌더`/`독해_선택` 작성. 데이터 스키마도 교체: `{지문, 질문, 유형, 보기:[{문장,
  정답,해설}]×4}`. 이름도 **"문단 빈칸 추론" → "지문 독해"** 로 전면 변경(홈 카드·타이틀·배지·
  모드_마스터리맵 키·JSON 훅 키(`문단_빈칸`→`지문_독해`) 전부).
- 임시 지문 4건 작성(요지 1·추론 2·세부 1), 전부 문장 간 통합이 있어야 정답이 나오도록 설계.
- 복습대기열(`복습대기열_추가`)은 "단어" 단위로 설계돼 있어 지문·질문 단위인 독해 문항에는 억지로
  끼워맞추지 않고 사용 안 함(정답/오답 EXP·마스터리 카운터·연속정답만 반영).
- **넛지(항목1)**: `예문형_선택`에서 근사(acceptable) 판정 시에만 결과 패널에 "지문 독해로 가기" 버튼
  노출(`.syn-nudge`). 근사=EXP 없음 정책·판정 로직은 그대로(사용자가 "폐지 대신 안내"로 확정).

### 보류
- 전용 업적(`문해력학습수` 기반) 정의는 콘텐츠가 더 채워진 뒤.
- `data/지문독해.json` 미생성(정적 표본 4건으로 동작). 채우면 `지문_독해` 키로 자동 교체.
- 색맹 접근성 텍스트 라벨, 랜덤학습 모드 목록에 지문 독해 추가 여부(이번엔 범위 밖 — `랜덤학습_모드목록`에
  sq6 미포함 확인, 필요 여부 사용자 확인 후 처리).
- 배너 시각·대비는 코드 검증 한계 — 실기기 확인 필요.

### 신규 코드 지점 (다음 세션 검색용)
- `독해_렌더` / `독해_선택` / `지문독해풀` / `.rc-passage` / `.rc-opt` / `.prof-banner` / `.banner-layer` /
  `settingsBanner` / `.syn-nudge` / `visibilitychange`+`pagehide` 리스너(채팅세션_마감 근처)

---

## [세션 10-d] 실기기 6차 피드백 7건 — 채팅 저장 견고화·순수텍스트·큰 팝업·배너 확대/크롭·카드 재편
> 세션10-c 직후. 소넷으로 구현(설계 판단은 앞서 오푸스 논의로 확정). 순서: ①채팅저장→②순수텍스트→③큰팝업→
> ④배너확대→⑤카드재편→⑥크롭UI. 검증: node --check·npm test(전체+신규 세션10-d 20건)·htmlhint 전부 통과.

### ① 채팅 저장 견고화 (항목1: "새 대화 눌러야 저장")
- 원인: 세션10-c의 `visibilitychange→채팅세션_마감`이 (a)로그인 Firestore 비동기 write를 언로드 시점에 완료
  못하고 (b)백그라운드 전환마다 채팅창을 초기화하는 부작용. → **언로드 타이밍 의존을 버림.**
- 해결: `채팅기록_추가메시지`가 매 메시지마다 `진행중세션_저장()`으로 진행 중 세션을 **동기 localStorage
  (`plx_진행중대화`)에 상시 백업**. `채팅세션_마감` 정상 종료 시 키 제거. `채팅기록_로드` 완료 후
  `진행중세션_복원()` — 키가 남아 있으면(=언로드로 중단) 앱 생존 중에 flush(Firestore 성공) + 중복(시작시각)
  방지. 언로드 리스너는 `진행중세션_저장()`만(마감·리셋 없음) → 백그라운드 전환해도 채팅창 유지.

### ② AI 메시지 순수 텍스트 저장 (항목2)
- 봉인 경로(구 `'Grok 연동 준비 중 안내'` 라벨)·활성 경로(구 `'[사고전개] '+…` 가공) → **화면에 뿌린 텍스트
  그대로 저장**(`<br>`만 개행 치환). 뷰어 `채팅세션_보기` 말풍선에 `white-space:pre-wrap`로 개행 보존.

### ③ 채팅 내역 큰 팝업 (항목3)
- `showInfoModal(icon,title,desc,와이드)` 4번째 인자 추가 → `.modal-bx.wide{max-width:560px}` 토글.
  `채팅내역_열기`·`채팅세션_보기`가 와이드로 열림, 내부 스크롤 `max-height:300px→62vh`.

### ④ 배너·프로필 확대 (항목4)
- `.prof-banner min-height 136→184px`, `.st-av`/`.set-av` 60·57→72px, `.st-name` 24→28px, `.set-nm` 20→25px
  (글자범위 전체 스케일 CSS도 28px로 동기화). 세션7 배너 테스트의 24px assert도 28px로 갱신.

### ⑤ 카드 재편 (항목6·7)
- 순서: **교정(맞춤법·구어교정) 맨 위** → 지식(상식·어원·세계사·신화) → 어휘(고사성어·한자) →
  문해(지문 독해) → 재미(아재개그) → 랜덤. `s1`~`s9` 순차 재부여.
- 색 그룹 확장: g4 교정=`--acc`, g5 재미=`--err`(기존 g1=--inf 지식·g2=--warn 어휘·g3=--ok 문해).
- **색맹 대비 그룹 라벨** `.mc-glabel`(각 그룹 색을 `--gl`로 텍스트에도 노출 — 색+두께+글자 3중 신호).
- `랜덤학습_모드목록`에 `['지문 독해','sq6']` 추가.

### ⑥ 본격 배너 크롭 UI (항목5, 센터피스)
- 기존 무조건 중앙 크롭(cover) 폐지 → 파일 선택 시 **크롭 모달(`cropBg`)** 로 진입.
- 뷰포트(표시 비율 700:320) 안에서 이미지를 cover 배치 후 **드래그(pointer 이벤트+setPointerCapture)로 이동
  + 슬라이더로 확대(1~3배)**. 항상 뷰포트를 덮도록 tx·ty·zoom clamp. 적용 시 700×320 캔버스에 보이는 영역만
  렌더 → `toDataURL(jpeg 0.82)`(Firestore 1MB 內) → 기존 `배너_적용선택` 재사용(현황·설정 동시 반영).
- getContext null 가드로 jsdom/캔버스 미지원 안전.
- 함수: `배너크롭_열기/중앙정렬/클램프/렌더/줌/드래그시작·중·끝/취소/적용`, 상태 `배너크롭_상태`.

### 보류
- `data/지문독해.json`·전용 업적(이전 세션부터 이월).
- 크롭 실기기 드래그 감도·핀치 줌(현재 슬라이더만) — 필요 시 후속.
- 배너 표시 비율(700:320)과 실제 카드 비율이 기기 폭에 따라 미세하게 달라 cover 소폭 조정 발생 가능 —
  사용자가 크롭으로 보정 가능하나, 신경 쓰이면 카드 비율 고정 검토.

### 신규 코드 지점 (다음 세션 검색용)
- `진행중세션_저장`/`진행중세션_복원`(`plx_진행중대화`) / `.modal-bx.wide` / `.mc-g4`·`.mc-g5`·`.mc-glabel` /
  `배너크롭_*` / `#cropBg`·`#cropViewport`·`#배너크롭이미지`·`#cropZoom` / `tests/test-fixes-세션10d.cjs`

---

## [세션 10-e] 실기기 7차 피드백 3건 — 초기화 버그·반응형 모달·랜덤 넘어가기
> 세션10-d 직후. 검증: node --check·npm test(전체+신규 세션10-e 15건)·htmlhint 전부 통과.

### ① 학습 데이터 초기화 — 이중확인 없음 + 실제로 초기화 안 됨 (항목1)
- **이중확인 부재**: `학습데이터초기화_1단계`가 `showConfirmModal` 1회로 바로 `_실행`을 부르고 있었음(계정삭제는
  `_1단계→_2단계→_실행` 2단계 체인). → 계정삭제와 동일한 2단계 체인으로 맞춤(`학습데이터초기화_2단계` 신설).
- **"실제로 초기화 안 됨"의 진짜 원인**: `사용자데이터_저장`이 `set({...},{merge:true})`라 `업적진행도:{}`
  (맵 필드)를 **재귀 병합**만 함 — 빈 객체로 덮어써도 서버에 남아있던 기존 업적 키가 안 지워져 새로고침하면
  업적이 되살아남. → 리셋 저장 뒤 `fbDb.doc(uid).update({업적진행도:{}})`를 별도 호출(update는 병합 없이
  필드를 통째로 덮어씀). 부차적으로 `afterNav(curScreen)`가 설정 화면 등에서 재렌더 분기가 없어 즉시 반영이
  안 보이던 문제도 `goNav('sh', null)`로 홈 이동시켜 해소.

### ② 반응형 모달 (항목2)
- `.modal-bx`(340px 고정)·`.modal-bx.wide`(560px 고정)이 PC 대형 화면에서 여백만 남기던 문제 →
  `clamp(320px, 46vw, 520px)` / `clamp(340px, 72vw, 680px)`로 반응형화. 폰은 하한값이 사실상 기존과 동일,
  PC는 뷰포트 비례로 확장. `.lvup-popup`·`.obj-modal` 등 다른 팝업은 이번 범위 밖(요청 없었음).

### ③ 랜덤 넘어가기 (항목3)
- 신규 플래그 `랜덤진입` — `goLearn` 진입부에서 매번 `false`로 리셋, `랜덤학습()`이 `goLearn` 호출 **뒤**에
  `true`로 세팅(순서 중요 — goLearn이 먼저 꺼버리므로 반드시 그 다음에 켜야 함).
- sq1~sq6 화면 하단(AI 챗 바 바로 위)에 `.random-skip` 버튼 6곳 동일 삽입, `랜덤진입`에 따라 표시 토글.
- `현재모드_다음출제()` 신설 — goLearn의 sq1~sq6 출제 분기와 같은 로직이지만 타이틀·채팅마감·popstate 기록
  등 진입 부작용 없이 문제만 재출제. **goLearn의 분기가 바뀌면 이 함수도 함께 맞춰야 함**(중복 로직 주의).
  `랜덤_넘어가기()`가 이걸 호출 — 랜덤진입 플래그는 유지되어 버튼이 계속 보임(연속 스킵 가능).

### 보류
- 반응형 모달 상한값(520px/680px)은 임의 선정 — 실기기·PC 확인 후 조정 여지.
- 랜덤 넘어가기는 "같은 모드 다음 문제"만 구현(다른 모드로 재추첨은 요청 시 후속).
- 이전 세션부터 이월: `data/지문독해.json`·전용 업적·색맹 라벨 문구 확정.

### 신규 코드 지점 (다음 세션 검색용)
- `학습데이터초기화_2단계` / `랜덤진입` / `현재모드_다음출제` / `랜덤넘어가기_표시` / `랜덤_넘어가기` /
  `.random-skip` / `tests/test-fixes-세션10e.cjs`

---

## [세션 10-f] 모바일 실기기 피드백 3건 — 화면크기 기본값·초기화 배너/프로필 누락·배너 크롭 버그
> 세션10-e 직후. 플랜 모드 없이 직접 조사·수정(범위가 명확한 소규모 버그 3건). 검증: node --check·
> npm test(전체+신규 세션10-f 12건)·htmlhint 전부 통과.
>
> 별도 안건: 이 세션 시작 직전 "줌인·아웃 렉, 주기율표 버튼 깜빡임" 피드백이 도착했다가 사용자가 즉시
> 요청을 취소(interrupt)했음 — 무엇을 가리키는지 불명확하고(이 앱에 "주기율표" 요소 없음) 취소된 요청이라
> 손대지 않음. 재현 필요하면 다음에 구체적으로 다시 알려달라고 해야 함.

### 항목1 — 화면 크기 기본값 90%
"모바일로 보니 100%가 생각보다 화면이 꽉 참" 피드백. `setFontScale` 자체는 그대로 두고(기존 저장값이
있으면 그 값을 존중), **저장값이 없을 때만** 90을 기본으로 적용하도록 세 지점 수정:
- Firestore 복원(`if(data.화면배율) setFontScale(...)` → `setFontScale(data.화면배율 || 90, true)`).
- 게스트 localStorage 복원(`if(저장화면)...` → `저장화면 ? parseInt(...) : 90`).
- `신규사용자_생성()`의 기본 데이터 객체에 `화면배율: 90` 명시적으로 추가.
- 설정 화면 HTML 기본 `on` 칩·안내 텍스트도 100%→90%로.

### 항목2 — 학습 데이터 초기화 시 배너·프로필 안 됨
`학습데이터초기화_실행`의 리셋 객체에 애초에 `배너이미지`/`프로필이미지` 필드가 빠져 있어 초기화해도
그대로 남아있던 버그. 리셋 객체에 `배너이미지:''`, `프로필이미지:'⚔️'`(신규사용자 기본값과 동일) 추가.
EXP·레벨과 달리 배너·아바타는 여러 화면에 각자 렌더된 DOM이라 `goNav('sh')`만으로는 갱신 안 됨 →
`아바타_적용`(homeAvatar·statusAvatar·settingsAvatar·nmAvatar)·`배너_적용`(statusBanner·settingsBanner)을
명시적으로 재호출해 즉시 반영.

### 항목3 — 배너 크롭 후 "다시 바꿀 수 없음" (근본 원인 특정)
`배너크롭_열기(dataURL)`가 이미지 `onload` 콜백 안에서 `cropViewport.getBoundingClientRect()`로 뷰포트
크기를 측정하는데, 이 값은 **모달 등장 CSS 트랜지션**(`.modal-bx{transform:scale(.85)→scale(1)}`, 0.35s)의
영향을 받는다. 이미지 디코딩이 트랜지션 완료보다 먼저 끝나면(흔한 케이스) 트랜지션 도중의 축소된 크기를
측정해버려 크롭 좌표(`s0`·`tx`·`ty`)가 전부 어긋나고, 적용 결과가 매번 깨진 이미지로 나와 "바꿀 수 없다"로
체감됐을 가능성이 큼. `getBoundingClientRect()` → `offsetWidth`/`offsetHeight`로 교체 — 이 값들은 레이아웃
박스 크기만 반영하고 CSS transform에 영향받지 않아 트랜지션 진행 상황과 무관하게 항상 정확한 최종 크기를
준다. (jsdom은 실제 레이아웃을 계산하지 않아 이 수정 자체의 시각적 재현은 실기기 확인이 필요 — 테스트는
"offsetWidth/Height를 쓰고 실제 getBoundingClientRect() 호출은 없다"는 구조만 검증.)

### 보류
- 인터럽트된 "줌·주기율표 깜빡임" 피드백 — 재현 정보 부족으로 미착수.
- 이전 세션부터 이월 항목들(데이터 자산·전용 업적 등) 그대로.

### 신규 코드 지점 (다음 세션 검색용)
- `tests/test-fixes-세션10f.cjs` / `배너크롭_열기`의 `offsetWidth`/`offsetHeight` 측정 로직

---

## [세션 10-g] 10-f 항목1 정정 + 화면·글자 크기 범위 조정
> 세션10-f 직후 사용자가 항목1 구현이 의도와 다르다고 정정. 검증: node --check·npm test(전체+세션10f
> 파일 재검증 20건)·htmlhint 전부 통과.

### 항목1 정정 — "기본값 90%"의 정확한 의미
10-f에서 구현한 "신규 사용자는 90% 칩이 기본 선택되고 표기도 90%"는 **사용자 의도와 다름**. 정정된 요구:
**설정 화면의 기본 선택칩·문구("100%")는 그대로 두고, 저장된 설정이 없는 사용자에 한해 실제 렌더링
배율(zoom)만 조용히 0.9를 적용** — 즉 "100%"라는 표기와 실제 배율(0.9) 사이 의도된 불일치.
- HTML 기본 칩·문구를 100%로 원복(`fontScaleTxt` 텍스트, `#fontScaleOpts` on 클래스).
- 부팅 복원 두 지점(Firestore·게스트 localStorage)에서 저장값이 없을 때 더 이상 `setFontScale(90,true)`를
  쓰지 않음(이 함수를 쓰면 칩·문구도 "90%"로 바뀌어버림) — 대신 `document.documentElement.style.zoom='0.9'`를
  직접 설정해 칩 동기화 로직을 건드리지 않음. 저장값이 있으면 기존처럼 `setFontScale(값, true)`로 정상 복원
  (그 경우엔 칩도 실제 값에 맞춰 정확히 동기화되는 게 맞음 — 불일치는 오직 "미저장 기본 상태"에서만).
- `신규사용자_생성()`에 추가했던 `화면배율: 90` 필드 제거 — 신규 사용자는 이 필드를 아예 저장하지 않음
  (미저장 상태 자체가 "조용한 기본 적용"의 조건이므로, 저장해버리면 다음 로드부터 오히려 setFontScale이
  호출되어 칩이 "90%"로 어긋나게 바뀜).

### 항목2 — 화면 크기 최대 150%
`화면크기단계` 배열에서 175·200 제거(`[70,80,90,100,110,125,150]`), 설정 화면 버튼도 동일하게 정리.

### 항목3 — 화면·글자 크기 최소 70%
`화면크기단계`·`글자크기단계` 배열 앞에 70 추가, 두 설정 화면 버튼 목록 모두에 70% 버튼 추가.

### 참고 — 왜 처음에 잘못 짚었나
"기본값 90으로 해"라는 문장을 "신규 사용자 초기 설정을 90%로"로 직역했으나, 실제로는 "표기·선택 상태는
100%를 유지한 채 내부적으로만 좀 더 작게 렌더링해달라"는 뜻이었음. 이런 종류의 모호성은 이후에도 재발할
수 있으니, 사용자 표현이 "기존 설정/표기를 바꾸는 것"인지 "표기는 그대로 두고 내부 동작만 바꾸는 것"인지
헷갈리면 구현 전에 먼저 확인하는 편이 안전함(이번엔 되돌리는 재작업이 발생했다).

### 보류
- 인터럽트된 "줌·주기율표 깜빡임" 피드백 여전히 미착수(재현 정보 부족).

### 신규 코드 지점 (다음 세션 검색용)
- `화면크기단계`(70~150) / `글자크기단계`(70~150) / 부팅 시 `document.documentElement.style.zoom='0.9'`
  직접 설정 지점(Firestore 복원·게스트 localStorage 복원 두 곳)

---

## [세션 10-h] AI 챗 입력창 — 모바일에서 Enter=줄바꿈, 전송은 버튼으로
> 검증: node --check·npm test(전체+신규 6건)·htmlhint 전부 통과.

"모바일에서 Shift+Enter를 누르기 매우 불편하니 대부분의 채팅 앱처럼 모바일은 예외 처리해달라" 피드백.
`#askInp`(AI 챗 입력창)만 해당 패턴이었음(다른 Enter=제출 입력들은 전부 한 줄 `<input>`이라 줄바꿈 개념이
없어 그대로 둠 — 직접입력_제출·아재_직접제출·복습_직접제출).

- `matchMedia('(pointer: coarse)')`로 터치 우선 기기 판별하는 `모바일_입력환경()` 신설.
- `질문입력_Enter처리(event)` 함수로 분리 — PC(정밀 포인터)에서만 기존처럼 Enter=전송(Shift+Enter=줄바꿈),
  모바일에서는 Enter를 그냥 기본 동작(줄바꿈)에 맡기고 `preventDefault()`도 `sendAsk()`도 호출하지 않음.
  전송은 이미 존재하던 `.ask-send` 버튼으로(기기 무관 항상 사용 가능).
- 부팅 시 기기에 맞춰 placeholder 문구도 다르게 설정(모바일: "전송 버튼으로 보내기" 안내).

### 신규 코드 지점 (다음 세션 검색용)
- `모바일_입력환경` / `질문입력_Enter처리` / `tests/test-fixes-세션10h.cjs`

---

## [세션 10-i] 배너 클릭 히트박스 확대 + 프로필 사진 크롭 UI 재활용
> 검증: node --check·npm test(전체 18묶음+신규 19건)·htmlhint 전부 통과.

### 항목1 — 배너 변경 클릭 히트박스가 너무 작음
원인: `.prof-banner` 카드 안에서 `.banner-layer`(배경, z-index:0)에만 `onclick`이 걸려 있었는데, 같은
카드의 `.prof-info`(이름·칭호 텍스트, z-index:2)가 `flex:1`로 넓게 깔려 있어 그 "빈 여백" 위에서는 클릭이
`.prof-info`(핸들러 없음)에서 그냥 소비되고 끝남 — DOM 이벤트 버블링은 실제 조상 체인을 타지 시각적
z-index 순서로 옆 형제에게 넘어가지 않기 때문. 결과적으로 배경이 보이는 좁은 틈에서만 클릭이 먹혔음.
- 탭 핸들러(`배너선택_열기()`)를 `.banner-layer`가 아니라 카드 전체(`.prof-banner`, 현황·설정 탭 둘 다)로
  이동 — 버블링으로 카드 어디를 눌러도 동작.
- 아바타(`#statusAvatar`/`#settingsAvatar`)와 설정 탭 "이름 변경" 버튼은 카드 안에 있으므로 각자 클릭이
  카드 클릭과 충돌하지 않도록 `onclick="event.stopPropagation();..."` 추가.
- `.prof-banner`에 `cursor:pointer` 추가(카드 전체가 클릭 가능해 보이도록 — 기존엔 `.banner-layer`에만
  있었음).

### 항목2 — 프로필 사진도 배너처럼 화면 잘라주는 기능 (기존 배너 크롭 코드 재활용)
기존 `프로필_파일처리`는 무조건 이미지 중앙을 128px 정사각으로 강제 크롭하는 별도 로직이었음. 요청대로
새로 만들지 않고 세션10-d에서 만든 배너 크롭 모달(드래그 이동 + 슬라이더 확대)을 **배너·프로필 공용**으로
일반화해서 재사용:
- `배너크롭_*` 함수군 전체를 `이미지크롭_*`로 개명하고, 상태 객체에 `대상:'배너'|'프로필'` 필드 추가.
- `이미지크롭_출력 = { 배너:{w:700,h:320}, 프로필:{w:256,h:256} }` — 대상별 출력 캔버스 크기만 다르고
  드래그·줌·클램프 수학은 완전히 동일(재사용).
- `이미지크롭_열기(dataURL, 대상)`가 대상에 따라 (a) 크롭 뷰포트에 `.round` 클래스 토글(프로필: 정사각
  뷰포트 + `box-shadow` 트릭으로 원형 마스크 시각 안내 — 실제 저장 파일은 그냥 정사각 JPEG이고 원형은
  아바타가 `border-radius:50%`로 렌더링될 때 자연히 둥글게 보임), (b) 모달 제목을 "배너 자르기"/
  "프로필 사진 자르기"로 전환.
- `이미지크롭_적용()`이 `대상`에 따라 `배너_적용선택(dataURL)` 또는 `프로필_적용선택(dataURL)`로 라우팅
  (두 적용 함수 자체는 기존 그대로 — Firestore 저장·DOM 갱신 로직 불변).
- `배너_파일처리`/`프로필_파일처리` 둘 다 이제 동일하게 `이미지크롭_열기(rd.result, '배너'|'프로필')` 호출.

### 보류
- 인터럽트된 "줌·주기율표 깜빡임" 피드백 여전히 미착수(재현 정보 부족, 이전 세션부터 이월).

### 신규 코드 지점 (다음 세션 검색용)
- `이미지크롭_*`(구 `배너크롭_*`) / `이미지크롭_출력` / `.crop-vp.round` CSS / `tests/test-fixes-세션10i.cjs`

---

## [세션 10-j] 창조주 업적 시도 채팅 미저장
> 검증: node --check·npm test(전체 19묶음)·htmlhint 전부 통과.

### 항목1 — 창조주 업적 진행을 위한 대화까지 채팅 기록에 저장되는 문제
원인 조사: 정확한 창조주 키(`창조주키`, 마지막 두 줄)와 완전히 일치하는 메시지는 원래도 `sendAsk()`의
별도 분기(6625~)에서 `채팅기록_추가메시지`를 아예 호출하지 않아 저장되지 않았음. 문제는 **일치하지
않는 시도**였음 — 업적 상세 화면의 "📋 키 문장 복사" 버튼(`복사_창조주키`, 5972)이 실제 매칭 대상인
마지막 두 줄이 아니라 **힌트 전체 5줄**(`부富와 권력權力…` 부터)을 클립보드에 복사하기 때문에, 사용자가
그대로 붙여넣으면 매칭 실패 → 일반 질문 분기로 떨어져 소설 인용문 원문이 그대로 채팅 기록에 저장되고
거기에 "Grok 연동 준비 중입니다" 같은 동문서답 AI 응답이 나란히 붙어 "이상한 기록"으로 남음. 오타·부분
복사 등 다른 실패 시도도 마찬가지 경로.
- `sendAsk()`에 `창조주시도중 = !사용자.창조주달성 && /[【】]/.test(q)` 플래그 추가 — `【】`는 창조주 키
  문장에서만 쓰이는 특수기호(앱 전체에서 다른 용도로 쓰인 곳 없음, grep으로 확인)라 오탐 위험이 낮음.
  이 플래그가 true면 화면(DOM)에는 기존처럼 메시지·응답을 그대로 보여주되(사용자 경험 유지),
  `채팅기록_추가메시지` 호출만 건너뛰어 정식 채팅 내역엔 남기지 않음 — 봉인 경로(안내문 응답)·Grok
  활성화 이후 경로(사고전개·rate_limit 포함 세 지점) 전부 동일 플래그로 일괄 가드.
- 창조주 **달성 후**에는 플래그 조건에 `!사용자.창조주달성`이 있어 자동으로 풀림 — 이후 인용문 등 정상
  대화까지 막지 않음.
- 근본 원인인 "복사 버튼이 5줄을 복사하는데 실제 매칭은 2줄"이라는 불일치 자체는 이번 요청 범위(채팅
  저장 방지) 밖이라 손대지 않음 — 필요하면 다음 세션에서 `복사_창조주키`가 마지막 두 줄만 복사하도록
  바꾸는 것도 고려 가능(사용자 확인 필요).

### 항목2 — 다크/라이트 모드 토글 처리 방향 (질문, 미착수)
"다크·라이트 토글이 사실상 페이퍼 테마로 바꾸는 정도밖에 안 되는데, 명암비까지 조절하는 기능으로
확장할지 아니면 토글 자체를 없앨지" 질문. 코드 확인 결과 `LIGHTS=['paper']`로 라이트 테마가 페이퍼
하나뿐이라 실제로 토글은 "마지막 다크 테마 ↔ 페이퍼" 전환에 불과함(설명 그대로). 구현 여부는 최고
관리자님 방향 결정 대기 중 — 아직 코드 변경 없음.

### 신규 코드 지점 (다음 세션 검색용)
- `sendAsk`의 `창조주시도중` 플래그 / `tests/test-fixes-세션10j.cjs`

---

## [세션 10-k] 창조주 채팅 미저장 가드 확대 + 다크/라이트 토글 삭제
> 실기기 재현 확인 후 항목1 추가 수정 + 항목2(질문) 방향 확정. 검증: node --check·npm test(전체
> 21묶음)·htmlhint 전부 통과.

### 항목1 — 세션10-j 수정에도 여전히 같은 문제 재현
실기기 스크린샷으로 확인: 이미 `[창조주]`를 달성한 계정에서 키 문장을 다시 입력(재확인·복사 오차 등으로
정확히 일치하지 않음)했을 때, `채팅기록_추가메시지` 호출을 걸러주던 세션10-j의 `창조주시도중` 플래그가
`!사용자.창조주달성` 조건 때문에 **달성 후에는 꺼져버려서** 여전히 매칭 실패 시 채팅 기록에 저장되고
있었음("맞춤법 대화" 채팅 내역에 키 문장 원문 + "Grok 연동 준비 중" 안내가 3번 연속으로 쌓인 스크린샷으로
확인).
- `창조주시도중` 판정에서 `!사용자.창조주달성` 조건을 제거 — 이제 달성 여부와 무관하게 `【】`가 포함된
  메시지는 항상 채팅 기록 저장을 건너뜀(화면 표시는 그대로 유지). `【】`는 이 앱에서 창조주 키 문장에만
  쓰이는 특수기호라 오탐 위험이 낮다는 전제는 세션10-j와 동일.
- (참고) 정확히 일치하는 키는 애초에 별도 분기에서 조기 return되어 원래도 저장 안 됨 — 이번 수정 대상은
  "일치하지 않는 시도"만 해당.

### 항목2 확정 — 다크/라이트 모드 토글 삭제
UX 관점 추천(정신모델 단순화·테마 그리드와 기능 중복) 전달 후 최고 관리자님 확정: **삭제**.
- 설정 화면 「외관」 섹션에서 "화면 모드" 행(체크박스 스위치 + 다크/라이트 라벨) 통째로 제거. 바로 아래
  「테마 선택」 그리드(5개 프리셋 + 커스텀)는 그대로 유지 — 이제 테마 전환의 유일한 경로.
  - `setTheme()`에서 `isLight` 판정·`마지막다크테마`/`마지막라이트테마` 기억·`modeToggle`/`lblD`/`lblL`/
    `modeSubTxt` DOM 갱신 로직 전부 제거(더 단순해짐).
  - `toggleMode()` 함수, `마지막다크테마`/`마지막라이트테마` 변수, `LIGHTS` 상수 전부 삭제(다른 곳에서
    쓰이지 않음, grep으로 확인).
  - `.mt`/`.mtt`/`.mtth`(스위치 컴포넌트 CSS)는 "AI 문제만 출제" 등 다른 토글이 공유해서 쓰므로 그대로 둠.
- KNOWLEDGE 18섹션(설정 화면 구조)에서 "화면 모드 (다크/라이트 토글)" 항목 삭제 반영.

### 신규 코드 지점 (다음 세션 검색용)
- `sendAsk`의 `창조주시도중`(달성 무관 상시 가드) / `setTheme`(토글 로직 제거로 단순화) /
  `tests/test-fixes-세션10j.cjs`(갱신) · `tests/test-fixes-세션10k.cjs`(신설)

---

## [세션 10-l] 계정 삭제·학습 데이터 초기화 시 채팅 기록 미삭제 수정
> 검증: node --check·npm test(전체 22묶음)·htmlhint 전부 통과.

### 문제 — 계정 삭제/학습 데이터 초기화를 해도 채팅 내역이 사라지지 않음
원인 조사: 두 흐름 다 채팅 기록(`채팅기록` 배열·Firestore `users/{uid}/채팅기록` 서브컬렉션·게스트
localStorage `plx_채팅기록`)을 전혀 건드리지 않고 있었음.
- `학습데이터초기화_실행()`은 레벨·EXP·업적·복습 대기열 등은 리셋 객체·Firestore `update()`로 확실히
  지우면서도 채팅 기록만 빠져 있었음(단순 누락).
- `계정삭제_실행()`은 Firestore `users/{uid}` 문서와 Firebase Auth 계정만 지웠는데, **Firestore
  서브컬렉션은 부모 문서를 지워도 자동으로 안 지워진다**(잘 알려진 Firestore 특성) — `채팅기록`
  서브컬렉션 문서들이 고아 데이터로 그대로 남고, 로컬 상태(`채팅기록` 배열·`현재채팅세션`·
  localStorage)도 초기화 없이 그대로라 화면상으로도 안 지워진 것처럼 보임.

### 수정
- 공용 헬퍼 `채팅기록_전체삭제()` 신설(`채팅기록_문서삭제` 바로 아래) — 로그인 사용자는 Firestore
  서브컬렉션 문서를 `채팅기록_문서삭제`로 개별 삭제(클라이언트 SDK엔 "서브컬렉션 통째 삭제" API가 없어
  기존 세션 정리 패턴과 동일하게 개별 삭제), `채팅기록` 배열·`현재채팅세션`을 비우고, localStorage
  `plx_채팅기록`·`plx_진행중대화`를 지운 뒤 `채팅창_초기화()`로 화면도 인사말로 리셋.
- `학습데이터초기화_실행()`에 `채팅기록_전체삭제()` 호출 추가(복습 대기열·휴지통 정리 직후).
- `계정삭제_실행()`에도 `채팅기록_전체삭제()` 호출 추가 — **반드시 Firestore 사용자 문서 삭제·Auth
  계정 삭제보다 먼저** 호출해야 함(Auth 계정이 지워지면 인증 토큰이 무효화돼 그 뒤엔 서브컬렉션 삭제
  요청 자체가 보안 규칙에 막힐 수 있음).

### 신규 코드 지점 (다음 세션 검색용)
- `채팅기록_전체삭제` / `학습데이터초기화_실행`·`계정삭제_실행` 호출 지점 / `tests/test-fixes-세션10l.cjs`

---

## [세션 10-m] 문해력 2탄 — D안 "문장 배열" 모드 신설 (sq7)
> 플랜 모드로 조사·설계 후 구현. 검증: node --check·npm test(전체 22묶음, test-learn.cjs 신규 12건 포함)·
> htmlhint 전부 통과.

### Context
세션10-b에서 검토한 문해력 후보 5가지(A~E) 중 C안(문단 빈칸 추론 → 지금의 sq6 "지문 독해")만 구현했고,
Opus의 원래 추천은 "C+D를 함께"(둘 다 Grok 불필요)였지만 D는 미착수로 남아 있었다(방금 세션에서 D·E안
상세를 대화 로그에서 복원해 위 세션10-b 기록에 채워 넣었다). 최고 관리자님이 "오푸스의 추천대로 D안도
지금 진행한다"고 확정 — **D안(문장 배열)**: 뒤섞인 문장을 원래 순서대로 재배열하는 문제. 지문 독해(요지·
추론·세부 일치)와 달리 **응집성·연결 논리**("그러나/결국/다행히" 같은 연결어로 흐름 좇기)를 훈련한다.

### 설계 판단
- **UI 방식**: 앱에 리스트 순서를 바꾸는(sortable/drag) 패턴이 전혀 없었다. 유일한 포인터 드래그 사례
  (이미지 크롭)는 "연속 좌표 이동"이지 "아이템 순서 교환"이라 재사용이 어려움. 대신 4지선다에서 이미
  성숙한 **번호 배지(`.onum`) + 탭 선택(`.aopt`)** 패턴을 재활용 — **"문장을 읽는 순서대로 하나씩 탭하면
  그 순번이 배지에 찍히는" 방식**을 채택(드래그 앤 드롭 아님). 모바일에서 스크롤과 충돌하는 `touch-action`
  이슈도 없다.
- **데이터 스키마 간소화**: 문장을 정답 순서로 저장 → 화면엔 셔플해서 보여주고, 탭한 순서의 원본
  인덱스열이 `[0,1,2,3]`이면 정답. sq6의 `보기[].정답:true/false` 같은 명시적 플래그가 필요 없음.
- **마스터리 카운터 신설 안 함**: `문해력학습수`를 지문 독해와 공유(고사성어·속담/한자·우리말이
  `언어의뿌리학습수`를 공유하는 선례와 동일 방침) — 사용자 기본 객체 3곳을 안 건드려도 됨.
- **CSS 재사용 극대화**: `.syn-card`/`.syn-result`/`.syn-actions`/`.aopt`/`.onum` 전부 그대로. 신규 CSS는
  홈 카드 10번째 스태거 딜레이 `.s10` 한 줄뿐.

### 구현
- **화면 sq7 DOM** — sq6 골격 그대로 복제(타이틀 "문장 배열", 배지 "🧩 문장 배열", `sq7Body`). `.lset-panel`
  없음(방식 옵션 없음, sq6과 동일 방침).
- **홈 카드** — 지문 독해 카드 바로 뒤, 같은 "📖 문해" 그룹(`mc-g3`)에 추가. 뒤따르던 아재개그·랜덤 카드의
  스태거 클래스를 s8→s9, s9→s10으로 한 칸씩 밈.
- **배선** — `SHOW_NAV`·`NAV_MAP`에 `sq7` 등록, `goLearn()`·`현재모드_다음출제()` 분기 추가, `모드_마스터리맵`에
  `'문장 배열':'문해력학습수'`, `랜덤학습_모드목록`에 `['문장 배열','sq7']`(랜덤 설정 패널에도 자동 반영 —
  `랜덤설정_열기`가 목록을 그대로 순회하는 구조라 별도 배선 불필요).
- **엔진** — `문장배열_렌더`(항목 무작위 선택 + 셔플, 우연히 정답 순서로 셔플되면 재셔플)/`문장배열_탭`
  (탭할 때마다 원본 인덱스를 순서열에 push, 배지에 순번 표시, 전부 탭하면 자동 판정)/`문장배열_초기화`
  (판정 전 "↺ 다시 배치"로 리셋)/`문장배열_판정`(전체 정답 여부 계산 + 문장별 위치가 맞았는지 개별
  `.correct`/`.wrong` 표시 + 오답 시 정답 순서 전체 나열 + 해설, EXP·마스터리·세션결과 반영).
- **데이터** — 임시 표본 3건("그러나/결국"·"그때마다/몇 주가 지나자"·"그럼에도/다행히" 연결어로 순서가
  명확한 4문장 미니 일화, 지문독해풀과 동일 rigor로 각 항목에 해설 포함).
- **JSON 로드 훅** — `예문데이터_로드`에 `d.문장_배열` 키(정령왕 JSON, 아직 값 없음 — 지문_독해와 동일하게
  키 없으면 무해).

### 보류
- `data/문장배열.json` 미생성 — 지문 독해도 아직 안 만든 상태라 동일 방침(정적 표본 3건으로 시작).
- 전용 업적 없음 — `문해력학습수` 공유 카운터로만 집계, 지문 독해와 함께 콘텐츠 채운 뒤 정의.
- E안(문장 고쳐쓰기)·A안(지문 독해 Q&A)·B안(요약 채점)은 여전히 Grok 해제 후 2단계.

### 신규 코드 지점 (다음 세션 검색용)
- `문장배열_렌더`/`문장배열_탭`/`문장배열_초기화`/`문장배열_판정` / `문장배열풀` / `sq7` 화면 /
  `.s10` CSS / `tests/test-learn.cjs`(sq7 케이스 12건)

---

## [세션 10-n] 문장 배열(sq7) 인터랙션 정정 — 탭 취소·제출 버튼·판정 애니메이션
> 세션10-m 직후 사용자 피드백 3건. 검증: node --check·npm test(전체 22묶음, test-learn.cjs sq7 관련
> 39건으로 확장)·htmlhint 전부 통과.

### 피드백
1. "문장 다시 클릭시 번호 매긴걸 취소 하는 기능 넣어" — 기존엔 탭한 문장이 즉시 `.disabled`(pointer-events
   차단)돼 다시 탭할 수 없었음.
2. "4개 다 클릭하면 바로 되는게 아니라 아래 제출하기 버튼 만들고 그걸 눌러야 될수 있게 해" — 기존엔 4번째
   탭과 동시에 자동 판정돼, 준비 안 된 상태에서 바로 채점되는 문제.
3. "제출시 보상을 얻게 되는데 이때의 애니메이션이 상당히 부자연스러워" — 원인 특정: 렌더 시 각 `.aopt`에
   걸어둔 fadeUp 등장 연출용 inline `animation-delay:0.05~0.2s`가 나중에 `.correct`/`.wrong` 클래스가
   붙을 때(`correctPop`/`shakeLR` 애니메이션) **그대로 재사용**돼, 4개 문장의 판정 연출이 0.05~0.2초씩
   밀려 순차적으로 재생됐음. 참고로 기존 4지선다(`selAns`)는 이 inline delay를 애초에 안 쓰고 CSS
   `.aopt:nth-child(N)` 셀렉터로만 등장 연출을 처리해 이 문제가 없었음 — sq7만 불필요하게 inline으로
   중복 지정했던 것.

### 수정
- **개별 취소**: `문장배열_탭`을 토글 방식으로 재작성 — 이미 탭순서에 있는 문장을 다시 탭하면 배열에서
  `splice`로 제거(취소), 없으면 끝에 추가. 번호 표시는 `문장배열_번호갱신()`(신설)이 매번 탭순서 배열
  전체를 기준으로 다시 그려서, 중간 항목을 취소하면 뒤 문장들의 번호가 자동으로 당겨진다. 판정 전에는
  `.disabled`를 걸지 않아(재탭 가능해야 하므로) 신규 CSS `.aopt.picked`(중립색 `--acc`, 아직 채점 전임을
  구분)로 "탭됨" 상태만 표시.
- **제출 버튼**: `문장배열_탭`에서 4개가 다 찼어도 더 이상 자동으로 `문장배열_판정()`을 부르지 않음.
  대신 "제출하기" 버튼(`#sq7SubmitBtn`, `.btn-acc.dim` 신규 CSS로 비활성 표현)을 추가 — 탭할 때마다
  `문장배열_번호갱신()`이 4개 다 찼는지 확인해 `.dim` 클래스를 토글. `문장배열_제출()`(신설)이 완성
  여부를 다시 한번 확인한 뒤에만 `문장배열_판정()`을 호출(방어적 가드 — 버튼이 비활성이어도 직접 호출
  가능성 대비).
- **판정 애니메이션**: `문장배열_렌더`에서 `.aopt`에 걸던 inline `style="animation-delay:..."`를 완전히
  제거 — 기존 CSS `.aopt:nth-child(1~4)` 규칙이 등장 연출을 그대로 담당하므로 시각적 차이는 없고, 대신
  판정 시점에 붙는 `.correct`/`.wrong` 애니메이션이 inline delay 없이 4개 동시에 자연스럽게 재생된다.

### 신규 코드 지점 (다음 세션 검색용)
- `문장배열_번호갱신`/`문장배열_제출`(신설) / `.aopt.picked`/`.btn-acc.dim` CSS /
  `tests/test-learn.cjs`(재탭 취소·중간 번호 당김·제출 게이팅·inline delay 부재 검증 추가)
