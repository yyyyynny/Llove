# CLAUDE.md — 코드네임: 언어

> Claude Code가 이 저장소에서 작업 시 반드시 먼저 읽는 파일.
> KNOWLEDGE v5.0 / SYSTEM v5.0과 함께 사용. 세션 인계는 `작업인계_노트.md` 참조.
> 최고 관리자: 성재훈
> 최종 업데이트: 2026년 6월 10일 (정식 빌드1 — Claude Code 세션 2 기준)

---

## 프로젝트 개요

한국어 어휘력·표현력 향상 웹앱. 단일 HTML 파일 구조.
현재 정식 빌드1 단계 (데모 종료). Firebase 연동 완료, **Grok은 게이트 봉인 상태(크레딧 미구매)**.

---

## ⚠️ Grok 게이트 (최고 관리자님 지시 — 2026-06-10)

xAI 크레딧 미구매 상태. `index.html`의 **`GROK_활성화 = false`** 상수가
모든 Grok 실호출을 봉인하고 있다 (네트워크 호출·토큰 차감 전부 차단).
질문하기·이의있음·구어교정·AI출제의 연결 지점은 전부 구현되어 있으며,
크레딧 구매 후 이 플래그만 `true`로 바꾸면 활성화된다.
**최고 관리자님 승인 없이 true로 변경 금지.**

---

## 파일 구조 (실제 저장소 기준 — 세션 2 갱신)

```
/
├── index.html                  # 정식 빌드1 본체 (단일 HTML, ~0.26MB)
├── CLAUDE.md                   # 이 파일
├── README.md
├── 언어_KNOWLEDGE_v5.md        # 전체 설계 문서 (최신)
├── 언어_SYSTEM_v5.md           # Claude 행동 지침 (최신)
├── 작업인계_노트.md             # 세션 간 인계 노트 (먼저 읽을 것)
└── data/
    ├── 정령왕_통합_v2.json      # 예문 데이터 52건 (구어 27·유의어 18·한문장 7) — 연동 완료
    ├── 상식어원.json            # 모드 DB — 현재 빈 파일 {"items":[]}. 채우면 자동 출제
    ├── 고사성어속담.json        # 〃
    ├── 세계사신화.json          # 〃
    ├── 아재개그.json            # 〃
    ├── 한자우리말.json          # 〃
    └── 맞춤법.json              # 〃
```

---

## 행동 규칙 (Claude Code 작업 시 필수 준수)

1. **호칭**: 최고 관리자님
2. **코드 주석**: 전부 한글
3. **커밋**: 승인 후 실행
4. **오류 분석**: 원인 먼저 보고 → 코드 작성
5. **확정 사항 임의 변경 금지** (KNOWLEDGE 참조)
6. **근거 없는 칭찬·불필요한 사과 금지**
7. **node --check 의무화** (JS 문법 검증)
8. **애니메이션**: fadeUp(수직)/slideIn(수평)만. 대각선·회전 완전 금지

---

## 정식 빌드1 구현 목표 — 진행 현황 (세션 2 기준)

| 항목 | 빌드1 목표 | 상태 |
|---|---|---|
| 구글 계정 | Firebase Authentication 실제 연결 | ✅ 완료 (세션 1) · 브라우저 실동작 미검증 |
| 사용자 데이터 | Firestore 실시간 연동 | ✅ 완료 — 로그인·생성·EXP·마스터리·설정 전부 저장 (세션 2) |
| AI API | Grok 4.1 Fast 실제 호출 | 🔒 게이트 봉인 — 연결 지점 구현 완료, `GROK_활성화=true`만 남음 |
| 교차검증 | Grok 리즈닝 high (이의있음!) | 🔒 게이트 봉인 — 멀티턴 차감·reasoning_note 패널 구현 완료 |
| 토큰 시스템 | 차감·복구·UI 전체 구현 | ✅ 완료 (세션 2 — KNOWLEDGE 32·35) |
| reasoning_note | 생성·저장·표시 | ⚡ 표시 경로 완료 / 생성·Firebase 저장은 Grok 활성화 후 |
| 개발자 모드 봉인 | 오버레이/실DB 분리 완전 구현 | ⏳ 미착수 (간이 스냅샷 유지) |
| 유의어 변별 출제 | 정령왕 JSON 52건 연동 | ✅ 완료 (세션 2 — 유의어 18·한문장 7·구어 27 전부) |
| 특정 계정 특혜 | 실험용 부계 계정 특혜 로직 | ⏳ 미착수 (계정 정보 미공개 상태라 구현 불가) |

---

## 1순위 버그 14건 — 전체 처리 완료

세션 1에서 12건(1·2·3·4·5·6·9·A·B·C·D·E) 수정, 버그 7(계정 하드코딩)은 Firebase 연동으로 흡수.
상세 내역은 `작업인계_노트.md` 참조.

---

## 추가 작업 (Claude Code 단계) — 진행 현황

- ✅ **눈누 CDN 폰트 전환** (v3.8에서 완료, 11.6MB → 0.16MB)
- ✅ **글꼴 샘플 2세트 번갈아가며**: 피차일반(음율) ↔ Betelgeuse(유우리) + 가사 출처 표기 (세션 2)
- ✅ **Firebase 연동** (Auth + Firestore) — 콘솔 측 보안 규칙·승인 도메인은 최고 관리자님 직접 설정 필요
- ✅ **계정 삭제 기능** (2중 확인)
- ✅ **AI 출제 분기 로직** — DB 40%/AI 60% 스켈레톤 (Grok 봉인 중엔 DB 폴백, 세션 2)
- ✅ **성장 상세 화면 (`sg`)** — 소칭호 로드맵 + EXP 진행 바 + 획득 안내 (세션 2)
- ✅ **토큰 UI 전체** — 잔량 바 + 차감 내역 드롭다운 + [? 차감 안내] 팝업 (세션 2)
- ✅ **레벨 시스템 v4.1 개편 (α1~α13)** — 소칭호 7단계·×1.123·Lv.70 캡·주신 글리치 등 (세션 2)
- ⏳ **GitHub Pages 배포 설정** — 저장소 Settings에서 활성화 필요 (코드 밖)

---

## 절대 고정 항목

- 바텀 네비 `#g-bnav`: `.screen` 밖 독립 위치 (z-index:200)
- 플래시카드 ①~⑥ 번호·레이블: 수정 금지
- Firebase 변수명: KNOWLEDGE 13섹션 한글 변수명
- localStorage 키: `plx_` 접두사 (현재 `plx_테마`·`plx_폰트` 사용 중)
- API 키: Cloudflare Workers만 (프론트 노출 금지)
- **`GROK_활성화` 플래그: 승인 없이 변경 금지**

---

## 기술 스택

| 항목 | 내용 |
|---|---|
| 배포 | GitHub Pages |
| 인증 | Firebase Auth (구글 로그인 강제) — Compat SDK 12.13.0 |
| DB | Firestore + GitHub JSON (data/ 폴더) |
| **메인 API** | **Grok 4.1 Fast 단일 (Cloudflare Workers 관리) — 게이트 봉인 중** |
| **교차검증** | **Grok 4.1 Fast 리즈닝 high (이의있음! 전용) — 게이트 봉인 중** |
| 서버리스 | Cloudflare Workers (API 키 관리 — Grok 포함) |
| 예문 데이터 | data/정령왕_통합_v2.json (52건, fetch 연동 완료) |
| 폰트 | CDN 4종 + 눈누 CDN 6종 |
| 광고 | 카카오 애드핏 (보류) |

---

## 설계 참고

- KNOWLEDGE v5.0: 전체 설계 문서 (최신) — `언어_KNOWLEDGE_v5.md`
- SYSTEM v5.0: Claude 행동 지침 (최신) — `언어_SYSTEM_v5.md`
- 보류 항목: KNOWLEDGE 21섹션 — 임의 확정 금지
- Claude Code 작업 목록: KNOWLEDGE 31섹션 + 37섹션(진행 현황) 참조
- 세션 인계: `작업인계_노트.md` (다음 세션은 이 파일부터 읽을 것)
