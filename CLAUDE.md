# CLAUDE.md — 코드네임: 언어

> Claude Code가 이 저장소에서 작업 시 반드시 먼저 읽는 파일.
> KNOWLEDGE v5.0 / SYSTEM v5.0과 함께 사용. 세션 인계는 `작업인계_노트.md` 참조.
> 최고 관리자: 성재훈
> 최종 업데이트: 2026년 7월 12일

---

## 프로젝트 개요

한국어 어휘력·표현력 향상 웹앱. **2026-07-12 재구조화 완료** — 단일 HTML(8,133줄)을
`index.html`(마크업) + `style.css` + `js/` 23개 분할 스크립트로 분리 (아래 "파일 구조" 참조).
정식 빌드1 단계. Firebase 연동 완료, Grok은 게이트 봉인 상태(크레딧 미구매).

---

## ⚠️ Grok 게이트 (최고 관리자님 지시)

xAI 크레딧 미구매 상태. `GROK_활성화 = false` 상수(`js/grok.js`)가 모든 Grok 실호출을 봉인.
**최고 관리자님 승인 없이 true로 변경 금지.**

## ⚠️ 음성 생성 게이트

`음성생성_활성화` 플래그(`js/grok.js`) — 콜롬비나 음성 TTS 봉인 골격. 외부 GPU 서버 준비 후에만 true.
Grok과 동일 정책. **모델·음성 파일은 레포에 절대 커밋 금지**(`.gitignore` 가드).

---

## 파일 구조

> ⚠️ 구조 변경 시 이 섹션을 최신 상태로 즉시 갱신할 것.
> index.html은 GitHub Pages 배포 진입점으로 그대로 유지.

**현재 (재구조화 완료 — 2026-07-19, '잇는' 포탈 추가 반영 실측)**
```
index.html    ← 화면 구조(마크업)만, 1,186줄 — style.css·js/*.js 참조
style.css     ← 전체 스타일 945줄 (테마 변수·컴포넌트·애니메이션)
js/           ← 로직 24개 파일, 합계 6,171줄 (아래 "JS 분할 구조 원칙" 필독)
├── 상태.js          # 사용자 런타임 상태·보관함 미러·상태 변수·폰트 목록
├── 데이터로드.js     # data/ JSON fetch (정령왕 통합 + 모드 DB 6종)
├── 화면전환.js       # slideIn/slideOut·네비 (goNav·goLearn·afterNav)
├── 온보딩.js         # 온보딩·이름 입력/변경
├── firebase.js      # 구글 로그인·Firestore 사용자 데이터 동기화
├── 레벨.js          # EXP 획득·레벨업·마스터리·학습일
├── grok.js          # Grok 게이트(GROK_활성화)·음성생성 TTS 봉인 골격(음성생성_활성화)
├── 토큰.js          # 토큰 차감·복구·잔량 바 UI
├── 퀴즈엔진.js       # 출제 공통 상태·AI 출제 분기·퀴즈 데이터·직접입력 공용
├── 플래시카드.js     # 플래시카드 ①~⑥
├── 유의어변별.js     # 예문형 출제 + 맥락형(예문 빈칸) 퀴즈 공용 엔진
├── 아재개그.js       # 아재개그 모드
├── 구어교정.js       # 구어 교정 + 음성 입력(Web Speech API)
├── 복습.js          # 복습·학습 설정 패널·복습 플레이·대기열/즐겨찾기/휴지통
├── 업적.js          # 업적 데이터·진행도 실계산·현황 트리·팝업
├── 채팅.js          # 질문하기·창조주 시나리오·채팅 기록·AI 지침
├── 테마.js          # 안내 모달·테마/폰트/설정·커스텀 테마 에디터
├── 슬라이드.js       # 「오늘 한 문장」 슬라이드
├── 개발자모드.js     # 개발자 모드 오버레이/실DB 분리
├── 이의있음.js       # 이의제기 컨텍스트(general/synonym/speak)
├── 커스터마이징.js    # 칭호 선택·프로필·배너·이미지 크롭
├── 성장상세.js       # 성장 상세 화면(sg) 렌더
├── 잇는.js          # '잇는' 포탈(이세계 진입) — 홈 포탈 아이콘 + '잇' 회전 퍼즐·해금(잇는개방)
└── 초기실행.js       # 부트스트랩(데이터 로드·plx 캐시·Firebase초기화) — 반드시 마지막 로드
```

> ⚠️ **회전 애니메이션 예외**: 행동 규칙 8번("회전 완전 금지")은 화면 전환·장식 트랜지션 대상.
> `js/잇는.js`의 '잇' 퍼즐 조각 90° 회전은 **게임(wchain)의 관문**이라 게임 예외로 허용한다
> (퍼즐의 본질). 일반 학습 UI엔 적용하지 않음. wchain 전체 설계는 `wchain/시스템.md` 참조.
> `wchain/` = 끝말잇기 이세계(별도 진입점, 같은 도메인이라 로그인 자동 공유) — 이식 진행 중.

**공통 (변경 없음)**
```
data/                     # 게임 콘텐츠 JSON 7종 (정령왕_통합_v2 + 모드 DB 6종)
docs/                     # 작업보고서·가이드
scripts/                  # 배포 전 점검, js/*.js 문법 검사
tests/                    # 테스트 코드 (.cjs)
README.md                 # 저장소 개요
언어_KNOWLEDGE_v5.md       # 전체 설계 문서
언어_SYSTEM_v5.md          # Claude 행동 지침
작업인계_노트.md            # 세션 간 인계 노트 (다음 세션 필독)
```

---

## JS 분할 구조 원칙 (재구조화 완료 — 이후 작업 시 준수 필수)

- 23개 파일은 **클래식 스크립트**로 전역 스코프를 공유한다(원본 단일 스크립트의 코드 순서를
  100% 보존한 슬라이스 분할). **`<script type="module">` 전환 금지** — ① HTML 전반의 인라인
  onclick 핸들러가 전역 함수를 참조하고, ② jsdom 테스트(`tests/*.cjs`)는 module을 실행하지 못한다.
- **로드 순서 = index.html의 `<script src>` 태그 순서.** 임의 재배열 금지. `초기실행.js`가
  항상 마지막(부트스트랩이 앞 파일들의 함수를 호출).
- 새 기능은 해당 기능 파일에 추가. 새 파일 추가 시 index.html 태그 + 위 "파일 구조" 목록을 함께
  갱신. **app.js 하나로 되뭉치기 금지.**
- 테스트·검사 인프라: `tests/load.cjs`가 style.css·js/*.js를 인라인 재주입해 단일 HTML처럼 실행,
  `scripts/check-inline-js.mjs`가 js/*.js 전수 `node --check`, `scripts/check-deploy.mjs`가
  참조 누락/고아 파일·게이트 플래그 존재를 검사한다.

## 디자인 일관성 원칙 (최우선 — 특히 UI 추가/재구조화 시)

새 UI를 만들거나 기존 UI를 옮길 때, **반드시 아래처럼 실제 존재하는 CSS 변수·클래스만 사용할 것.**
새 변수명·클래스명·임의 색상값 도입 금지. (아래 목록은 2026-07-12 기준 실측 — 재구조화로 스타일이
`style.css`로 이동했으니 `grep -oE -- "--[a-z0-9-]+" style.css | sort -u`로 최신 목록 재확인 가능.)

**색/타이포 CSS 변수** (`:root` 및 `[data-theme="..."]` 블록에 테마별로 정의됨):
```css
--bg          /* 배경 */
--card        /* 카드 배경 */
--elev        /* 살짝 띄운 배경(카드보다 밝음) */
--bdr         /* 기본 테두리 */
--bdrg        /* 강조 테두리(accent 섞임) */
--acc         /* 액센트(포인트 컬러) */
--accl        /* 액센트 밝은 톤 */
--accd        /* 액센트 어두운 톤 */
--txt         /* 본문 글자색 */
--txt2        /* 보조 글자색 */
--txtm        /* 더 옅은(muted) 글자색 */
--ok          /* 정답/성공 색 */
--err         /* 오답/에러 색 */
--warn        /* 경고/근사 색 */
--inf         /* 정보 색(지식 계열 카드 등) */
--fn          /* 본문 폰트 */
--mono        /* 고정폭 폰트 */
--exp         /* EXP 바 그라디언트 */
```
> 참고: `--c-bg`/`--c-card`/`--c-acc`/`--c-txt`/`--c-fn` 5개는 커스텀 테마 에디터가 런타임에
> 덮어쓰는 **raw 입력값**이고(`document.documentElement.style.setProperty('--c-bg', ...)` 형태),
> 위 파생 토큰들이 `var(--bg, var(--c-bg, ...))` 식으로 이를 참조한다. **컴포넌트 CSS는 항상 위
> 파생 토큰(`--bg`, `--acc` 등)을 쓰고, `--c-*` 5개를 직접 참조하지 않는다.**

**기존 컴포넌트 클래스 예시** (새 UI는 이런 기존 클래스와 나란히 놓아도 이질감 없어야 함):
```
.modal-bx, .modal-bg          — 모달 박스/배경
.btn-g, .btn-acc               — 보조/강조 버튼
.set-row, .set-sec, .set-lbl   — 설정 화면 행/섹션
.mt, .mtt, .mtw                — 토글 스위치(라벨+원형 손잡이)
.fs-opt                        — 단계형 선택 버튼(화면/글자 크기 등)
.aopt, .onum                   — 번호 배지가 붙은 탭-선택 옵션
.syn-card, .syn-opt, .syn-result, .syn-actions  — 예문형/판정 결과 카드류
.mc, .mc-g1~.mc-g5, .mc-glabel — 홈 학습 모드 카드·색 그룹
```
border-radius·padding·transition 속도도 기존 패턴을 따를 것. 다크/라이트(테마 5종 + 커스텀) 전부
정상 동작 확인 필수.

---

## 재구조화·대규모 작업 시 준수 사항

1. **배치 진행**: 큰 작업(전체 재구조화, `data/` JSON 콘텐츠 채우기 등)은 한 번에 처리하지 말고
   배치로 나눠 진행할 것.
2. **배치마다 커밋**: 각 배치 완료 시 바로 커밋할 것 (컨텍스트 단절 시 git log로 진행 상황 파악
   가능하게).
3. **구조 변경 후 필수**: 기존 기능(음성 인식·커스텀 테마·Firebase 연동·`GROK_활성화`/
   `음성생성_활성화` 플래그 등) 전부 정상 작동하는지 검증하고, `tests/load.cjs`(style.css·js/*.js를
   인라인 재주입해 jsdom으로 실행)와 `scripts/check-inline-js.mjs`(js/*.js 전수 문법 검사)를 새
   파일 구조에 맞게 함께 갱신하고, 이 CLAUDE.md의 "파일 구조" 섹션을 실제 구조로 갱신할 것.
4. **사실 정확성**: 불확실한 사실(줄 수·변수명·클래스명·수치 등)은 추측하지 말고 실제 코드를
   검색으로 확인 후 작성할 것 — 다른 저장소·다른 프로젝트의 수치나 관례를 혼동해 옮겨적지 말 것.

---

## 행동 규칙 (Claude Code 작업 시 필수 준수)

1. **호칭**: 최고 관리자님
2. **코드 주석·커밋 메시지·발화**: 전부 한글
3. **커밋**: 승인 후 실행
4. **오류 분석**: 원인 먼저 보고 → 코드 작성
5. **확정 사항 임의 변경 금지** (KNOWLEDGE 참조)
6. **근거 없는 칭찬·불필요한 사과 금지**
7. **node --check 의무화** (JS 문법 검증)
8. **애니메이션**: fadeUp(수직)/slideIn(수평)만. 대각선·회전 완전 금지
9. **작성함/검증함 구분**: 실행·테스트하지 않은 코드는 "미검증"이라 명시

---

## 절대 고정 항목

- 바텀 네비 `#g-bnav`: `.screen` 밖 독립 위치 (z-index:200)
- 플래시카드 ①~⑥ 번호·레이블: 수정 금지
- Firebase 변수명: KNOWLEDGE 13섹션 한글 변수명
- localStorage 키: `plx_` 접두사
- API 키: Cloudflare Workers만 (프론트 노출 금지)
- **`GROK_활성화` 플래그: 승인 없이 변경 금지**
- **`음성생성_활성화` 플래그: 승인 없이 변경 금지**
- **CI/Node.js 버전: Node 24**

---

## 기술 스택

| 항목 | 내용 |
|---|---|
| 배포 | GitHub Pages |
| 인증 | Firebase Auth (구글 로그인 강제) — Compat SDK 12.13.0 |
| DB | Firestore + GitHub JSON (data/ 폴더) |
| 메인 API | Grok 4.1 Fast (Cloudflare Workers 관리) — 게이트 봉인 중 |
| 서버리스 | Cloudflare Workers (API 키 관리) |
| 폰트 | CDN 4종 + 눈누 CDN 6종 |

---

## 설계 참고

- KNOWLEDGE v5.0: `언어_KNOWLEDGE_v5.md`
- SYSTEM v5.0: `언어_SYSTEM_v5.md`
- 세션 인계: `작업인계_노트.md` (다음 세션은 이 파일부터 읽을 것)
